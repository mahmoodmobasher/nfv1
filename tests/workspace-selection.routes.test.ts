import{describe,expect,it}from"vitest";import{POST}from"../src/app/api/workspaces/switch/route";import{privateWorkspaceJson}from"../src/server/workspaces/http";
describe("workspace switch route guard",()=>{
 it("rejects missing CSRF before database work with a private response",async()=>{const response=await POST(new Request("http://127.0.0.1:3000/api/workspaces/switch",{method:"POST",body:"{}"}));expect(response.status).toBe(403);expect(response.headers.get("cache-control")).toBe("private, no-store");expect(await response.json()).toMatchObject({code:"request_rejected"})});
 it("rejects a cross-origin request even with matching CSRF values",async()=>{const response=await POST(new Request("http://127.0.0.1:3000/api/workspaces/switch",{method:"POST",headers:{origin:"https://attacker.invalid","content-type":"application/json","x-csrf-token":"same",cookie:"nexaflow_csrf=same"},body:"{}"}));expect(response.status).toBe(403);expect(response.headers.get("cache-control")).toBe("private, no-store");expect(await response.json()).toMatchObject({code:"request_rejected"})});
 it("marks Workspace shell JSON private on success and denial",async()=>{for(const response of[privateWorkspaceJson({workspaces:[]}),privateWorkspaceJson({code:"authentication_required"},{status:401})])expect(response.headers.get("cache-control")).toBe("private, no-store")});
});
