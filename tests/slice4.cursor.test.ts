import{describe,expect,it}from"vitest";import{decodeCursor,encodeCursor}from"../src/server/tenant-admin/cursor";
const secret="cursor-test-secret-at-least-32-characters",workspace="00000000-0000-4000-8000-000000000001",other="00000000-0000-4000-8000-000000000002",payload={endpoint:"people",workspaceId:workspace,createdAt:"2026-08-20T12:00:00.000Z",id:"00000000-0000-4000-8000-000000000003"};
describe("Slice 4 signed cursors",()=>{it("round trips stable endpoint/workspace pagination state",()=>{const cursor=encodeCursor(payload,secret);expect(decodeCursor(cursor,"people",workspace,secret)).toMatchObject(payload)});it("rejects tampering",()=>{const cursor=encodeCursor(payload,secret),tampered=`${cursor.slice(0,-1)}${cursor.endsWith("a")?"b":"a"}`;expect(()=>decodeCursor(tampered,"people",workspace,secret)).toThrow("validation_failed")});it("rejects endpoint reuse",()=>{expect(()=>decodeCursor(encodeCursor(payload,secret),"teams",workspace,secret)).toThrow("validation_failed")});it("rejects workspace reuse",()=>{expect(()=>decodeCursor(encodeCursor(payload,secret),"people",other,secret)).toThrow("validation_failed")})});
