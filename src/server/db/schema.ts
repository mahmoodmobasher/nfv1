import { sql } from "drizzle-orm";
import {
  bigint,
  boolean,
  char,
  check,
  date,
  foreignKey,
  index,
  integer,
  jsonb,
  numeric,
  pgTable,
  primaryKey,
  text,
  timestamp,
  uniqueIndex,
  uuid,
} from "drizzle-orm/pg-core";

const id = () => uuid("id").defaultRandom().primaryKey();
const timestamps = {
  createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull(),
};

export const users = pgTable(
  "users",
  {
    id: id(),
    primaryEmailNormalized: text("primary_email_normalized").unique(),
    primaryEmailDisplay: text("primary_email_display"),
    displayName: text("display_name").notNull(),
    status: text("status").notNull().default("pending_verification"),
    securityVersion: integer("security_version").notNull().default(1),
    emailVerifiedAt: timestamp("email_verified_at", { withTimezone: true }),
    ...timestamps,
  },
  (table) => [
    check("users_status_check", sql`${table.status} in ('pending_verification', 'active', 'suspended', 'deleted')`),
  ],
);

export const userPreferences = pgTable(
  "user_preferences",
  {
    userId: uuid("user_id").primaryKey().references(() => users.id, { onDelete: "cascade" }),
    appearance: text("appearance").notNull().default("system"),
    locale: text("locale"),
    timeZone: text("time_zone"),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    check("user_preferences_appearance_check", sql`${table.appearance} in ('system', 'light', 'dark')`),
    check("user_preferences_locale_check", sql`${table.locale} is null or length(btrim(${table.locale})) between 2 and 35`),
    check("user_preferences_time_zone_check", sql`${table.timeZone} is null or length(btrim(${table.timeZone})) between 1 and 64`),
    check("user_preferences_version_check", sql`${table.version} > 0`),
  ],
);

export const identityCredentials = pgTable(
  "identity_credentials",
  {
    id: id(),
    userId: uuid("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    provider: text("provider").notNull(),
    providerSubject: text("provider_subject").notNull(),
    passwordHash: text("password_hash"),
    lastUsedAt: timestamp("last_used_at", { withTimezone: true }),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("identity_provider_subject_uq").on(table.provider, table.providerSubject),
    uniqueIndex("identity_password_user_uq").on(table.userId).where(sql`${table.provider} = 'password'`),
    check("identity_credentials_provider_check", sql`${table.provider} in ('password', 'google')`),
  ],
);

export const workspaces = pgTable(
  "workspaces",
  {
    id: id(),
    name: text("name").notNull(),
    slug: text("slug").notNull().unique(),
    status: text("status").notNull().default("provisioning"),
    planCode: text("plan_code").notNull(),
    billingCadence: text("billing_cadence").notNull(),
    trialStartedAt: timestamp("trial_started_at", { withTimezone: true }),
    trialEndsAt: timestamp("trial_ends_at", { withTimezone: true }),
    createdByUserId: uuid("created_by_user_id").notNull().references(() => users.id),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    check("workspaces_status_check", sql`${table.status} in ('provisioning', 'active', 'suspended', 'closed')`),
    check("workspaces_billing_cadence_check", sql`${table.billingCadence} in ('monthly', 'annual', 'sales_managed')`),
    check("workspaces_version_check", sql`${table.version} > 0`),
  ],
);

export const roles = pgTable(
  "roles",
  {
    id: id(),
    workspaceId: uuid("workspace_id").references(() => workspaces.id, { onDelete: "cascade" }),
    code: text("code").notNull(),
    permissions: jsonb("permissions").notNull().default({}),
    policyVersion: text("policy_version").notNull().default("tenant-admin-v1"),
    version: integer("version").notNull().default(1),
    isSystem: boolean("is_system").notNull().default(false),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("roles_workspace_code_uq").on(table.workspaceId, table.code),
    uniqueIndex("roles_workspace_id_id_uq").on(table.workspaceId, table.id),
    check("roles_code_check", sql`${table.code} in ('owner', 'admin', 'member')`),
    check("roles_version_check", sql`${table.version} > 0`),
  ],
);

export const workspaceMemberships = pgTable(
  "workspace_memberships",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    userId: uuid("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    roleId: uuid("role_id").notNull(),
    status: text("status").notNull().default("active"),
    joinedAt: timestamp("joined_at", { withTimezone: true }).defaultNow().notNull(),
    removedAt: timestamp("removed_at", { withTimezone: true }),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("membership_workspace_user_uq").on(table.workspaceId, table.userId),
    uniqueIndex("membership_workspace_id_id_uq").on(table.workspaceId, table.id),
    index("membership_workspace_idx").on(table.workspaceId),
    foreignKey({
      name: "membership_workspace_role_fk",
      columns: [table.workspaceId, table.roleId],
      foreignColumns: [roles.workspaceId, roles.id],
    }),
    check("workspace_memberships_status_check", sql`${table.status} in ('active', 'suspended', 'removed')`),
    check("workspace_memberships_version_check", sql`${table.version} > 0`),
  ],
);

export const teams = pgTable(
  "teams",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    nameNormalized: text("name_normalized").notNull(),
    status: text("status").notNull().default("active"),
    version: integer("version").notNull().default(1),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("teams_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("teams_workspace_name_uq").on(table.workspaceId, table.nameNormalized),
    foreignKey({ name: "teams_workspace_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("teams_status_check", sql`${table.status} in ('active', 'archived')`),
    check("teams_version_check", sql`${table.version} > 0`),
    check("teams_name_check", sql`length(btrim(${table.name})) between 1 and 100 and ${table.nameNormalized}=lower(btrim(${table.nameNormalized}))`),
  ],
);

export const teamMemberships = pgTable(
  "team_memberships",
  {
    workspaceId: uuid("workspace_id").notNull(),
    teamId: uuid("team_id").notNull(),
    workspaceMembershipId: uuid("workspace_membership_id").notNull(),
    version: integer("version").notNull().default(1),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    ...timestamps,
  },
  (table) => [
    primaryKey({ name: "team_memberships_pk", columns: [table.teamId, table.workspaceMembershipId] }),
    uniqueIndex("team_memberships_workspace_uq").on(table.workspaceId, table.teamId, table.workspaceMembershipId),
    foreignKey({ name: "team_memberships_workspace_team_fk", columns: [table.workspaceId, table.teamId], foreignColumns: [teams.workspaceId, teams.id] }),
    foreignKey({ name: "team_memberships_workspace_member_fk", columns: [table.workspaceId, table.workspaceMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "team_memberships_workspace_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("team_memberships_version_check", sql`${table.version} > 0`),
  ],
);

export const workspaceInvitations = pgTable(
  "workspace_invitations",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    emailNormalized: text("email_normalized").notNull(),
    emailDisplay: text("email_display").notNull(),
    roleId: uuid("role_id").notNull(),
    status: text("status").notNull().default("pending"),
    tokenHash: text("token_hash").notNull().unique(),
    tokenGeneration: integer("token_generation").notNull().default(1),
    version: integer("version").notNull().default(1),
    expiresAt: timestamp("expires_at", { withTimezone: true }).notNull(),
    lastSentAt: timestamp("last_sent_at", { withTimezone: true }).notNull(),
    acceptedAt: timestamp("accepted_at", { withTimezone: true }),
    acceptedByUserId: uuid("accepted_by_user_id").references(() => users.id),
    acceptedMembershipId: uuid("accepted_membership_id"),
    revokedAt: timestamp("revoked_at", { withTimezone: true }),
    revokedByMembershipId: uuid("revoked_by_membership_id"),
    invitedByMembershipId: uuid("invited_by_membership_id").notNull(),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("workspace_invitations_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("workspace_invitations_pending_email_uq").on(table.workspaceId, table.emailNormalized).where(sql`${table.status}='pending'`),
    index("workspace_invitations_status_idx").on(table.workspaceId, table.status, table.createdAt, table.id),
    index("workspace_invitations_email_idx").on(table.workspaceId, table.emailNormalized),
    index("workspace_invitations_expiry_idx").on(table.status, table.expiresAt),
    foreignKey({ name: "workspace_invitations_role_fk", columns: [table.workspaceId, table.roleId], foreignColumns: [roles.workspaceId, roles.id] }),
    foreignKey({ name: "workspace_invitations_inviter_fk", columns: [table.workspaceId, table.invitedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "workspace_invitations_revoker_fk", columns: [table.workspaceId, table.revokedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "workspace_invitations_accepted_membership_fk", columns: [table.workspaceId, table.acceptedMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("workspace_invitations_status_check", sql`${table.status} in ('pending','accepted','revoked','expired','superseded')`),
    check("workspace_invitations_email_check", sql`length(${table.emailNormalized}) between 3 and 320 and ${table.emailNormalized}=lower(btrim(${table.emailNormalized}))`),
    check("workspace_invitations_generation_check", sql`${table.tokenGeneration}>0 and ${table.version}>0`),
    check("workspace_invitations_expiry_check", sql`${table.expiresAt}>${table.createdAt}`),
    check("workspace_invitations_terminal_check", sql`(${table.status}='accepted' and ${table.acceptedAt} is not null and ${table.acceptedByUserId} is not null and ${table.acceptedMembershipId} is not null and ${table.revokedAt} is null and ${table.revokedByMembershipId} is null) or (${table.status}='revoked' and ${table.revokedAt} is not null and ${table.revokedByMembershipId} is not null and ${table.acceptedAt} is null and ${table.acceptedByUserId} is null and ${table.acceptedMembershipId} is null) or (${table.status} in ('pending','expired','superseded') and ${table.acceptedAt} is null and ${table.acceptedByUserId} is null and ${table.acceptedMembershipId} is null and (${table.status}<>'pending' or (${table.revokedAt} is null and ${table.revokedByMembershipId} is null)))`),
  ],
);

export const workspaceInvitationTeams = pgTable(
  "workspace_invitation_teams",
  {
    workspaceId: uuid("workspace_id").notNull(),
    invitationId: uuid("invitation_id").notNull(),
    teamId: uuid("team_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "workspace_invitation_teams_pk", columns: [table.invitationId, table.teamId] }),
    uniqueIndex("workspace_invitation_teams_workspace_uq").on(table.workspaceId, table.invitationId, table.teamId),
    foreignKey({ name: "workspace_invitation_teams_invitation_fk", columns: [table.workspaceId, table.invitationId], foreignColumns: [workspaceInvitations.workspaceId, workspaceInvitations.id] }),
    foreignKey({ name: "workspace_invitation_teams_team_fk", columns: [table.workspaceId, table.teamId], foreignColumns: [teams.workspaceId, teams.id] }),
  ],
);

export const pipelineStages = pgTable(
  "pipeline_stages",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    position: integer("position").notNull(),
    status: text("status").notNull().default("active"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("pipeline_stages_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("pipeline_stages_workspace_position_uq").on(table.workspaceId, table.position),
    check("pipeline_stages_name_check", sql`length(btrim(${table.name})) between 1 and 80`),
    check("pipeline_stages_position_check", sql`${table.position} >= 0`),
    check("pipeline_stages_status_check", sql`${table.status} in ('active', 'archived')`),
  ],
);

export const leadLifecycleDefinitions = pgTable(
  "lead_lifecycle_definitions",
  {
    id: id(),
    code: text("code").notNull(),
    label: text("label").notNull(),
    displayOrder: integer("display_order").notNull(),
    isTerminal: boolean("is_terminal").notNull().default(false),
    status: text("status").notNull().default("active"),
    contractVersion: text("contract_version").notNull().default("p1a-lifecycle-v1"),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("lead_lifecycle_definitions_code_uq").on(table.code),
    uniqueIndex("lead_lifecycle_definitions_order_uq").on(table.displayOrder),
    check("lead_lifecycle_definitions_code_check", sql`${table.code} in ('new','working','qualified','disqualified','converted')`),
    check("lead_lifecycle_definitions_label_check", sql`length(btrim(${table.label})) between 1 and 80`),
    check("lead_lifecycle_definitions_order_check", sql`${table.displayOrder} >= 0`),
    check("lead_lifecycle_definitions_status_check", sql`${table.status} in ('active','archived')`),
    check("lead_lifecycle_definitions_version_check", sql`${table.version} > 0`),
  ],
);

export const companies = pgTable(
  "companies",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    displayName: text("display_name").notNull(),
    nameNormalized: text("name_normalized").notNull(),
    domainNormalized: text("domain_normalized"),
    websiteUrl: text("website_url"),
    industry: text("industry"),
    sizeBand: text("size_band"),
    employeeCount: integer("employee_count"),
    annualRevenueMinor: numeric("annual_revenue_minor", { precision: 20, scale: 0 }),
    annualRevenueCurrencyCode: char("annual_revenue_currency_code", { length: 3 }),
    annualRevenueCurrencyExponent: integer("annual_revenue_currency_exponent"),
    parentCompanyId: uuid("parent_company_id"),
    phoneDisplay: text("phone_display"),
    street: text("street"),
    city: text("city"),
    stateProvince: text("state_province"),
    postalCode: text("postal_code"),
    country: text("country"),
    normalizationVersion: text("normalization_version").notNull().default("p1a-identity-v1"),
    status: text("status").notNull().default("active"),
    version: integer("version").notNull().default(1),
    responsibleMembershipId: uuid("responsible_membership_id"),
    responsibleTeamId: uuid("responsible_team_id"),
    visibility: text("visibility").notNull().default("workspace"),
    governingOperationId: uuid("governing_operation_id"),
    createdByMembershipId: uuid("created_by_membership_id"),
    updatedByMembershipId: uuid("updated_by_membership_id"),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    authorityContractVersion: text("authority_contract_version").notNull().default("legacy-p1a-root-v1"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("companies_workspace_id_id_uq").on(table.workspaceId, table.id),
    index("companies_workspace_name_idx").on(table.workspaceId, table.nameNormalized, table.id),
    index("companies_workspace_domain_idx").on(table.workspaceId, table.domainNormalized, table.id),
    index("companies_parent_lookup_idx").on(table.workspaceId, table.parentCompanyId, table.id).where(sql`${table.parentCompanyId} is not null`),
    index("companies_default_list_idx").on(table.workspaceId, table.status, table.updatedAt.desc().nullsLast(), table.id.desc().nullsLast()),
    index("companies_responsible_membership_idx").on(table.workspaceId, table.responsibleMembershipId, table.status, table.updatedAt.desc().nullsLast(), table.id.desc().nullsLast()).where(sql`${table.responsibleMembershipId} is not null`),
    index("companies_responsible_team_idx").on(table.workspaceId, table.responsibleTeamId, table.status, table.updatedAt.desc().nullsLast(), table.id.desc().nullsLast()).where(sql`${table.responsibleTeamId} is not null`),
    foreignKey({ name: "companies_responsible_membership_fk", columns: [table.workspaceId, table.responsibleMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "companies_parent_company_fk", columns: [table.workspaceId, table.parentCompanyId], foreignColumns: [table.workspaceId, table.id] }).onDelete("no action"),
    foreignKey({ name: "companies_responsible_team_fk", columns: [table.workspaceId, table.responsibleTeamId], foreignColumns: [teams.workspaceId, teams.id] }),
    foreignKey({ name: "companies_creator_membership_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "companies_updater_membership_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "companies_archiver_membership_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("companies_name_check", sql`length(btrim(${table.displayName})) between 1 and 200 and length(${table.nameNormalized}) between 1 and 200`),
    check("companies_profile_fields_check", sql`(${table.websiteUrl} is null or (char_length(${table.websiteUrl}) between 1 and 500 and ${table.websiteUrl}=btrim(${table.websiteUrl}) and ${table.websiteUrl} !~ '[[:cntrl:]]')) and (${table.industry} is null or (char_length(${table.industry}) between 1 and 120 and ${table.industry}=btrim(${table.industry}) and ${table.industry} !~ '[[:cntrl:]]')) and (${table.phoneDisplay} is null or (char_length(${table.phoneDisplay}) between 1 and 50 and ${table.phoneDisplay}=btrim(${table.phoneDisplay}) and ${table.phoneDisplay} !~ '[[:cntrl:]]'))`),
    check("companies_size_band_check", sql`${table.sizeBand} is null or ${table.sizeBand} in ('micro','small','medium','large','enterprise')`),
    check("companies_employee_count_check", sql`${table.employeeCount} is null or ${table.employeeCount} between 0 and 2147483647`),
    check("companies_annual_revenue_check", sql`(num_nonnulls(${table.annualRevenueMinor},${table.annualRevenueCurrencyCode},${table.annualRevenueCurrencyExponent})=0) or (num_nonnulls(${table.annualRevenueMinor},${table.annualRevenueCurrencyCode},${table.annualRevenueCurrencyExponent})=3 and ${table.annualRevenueMinor} between 0 and 99999999999999999999 and ${table.annualRevenueCurrencyCode} in ('USD','CAD') and ${table.annualRevenueCurrencyExponent}=2)`),
    check("companies_parent_self_check", sql`${table.parentCompanyId} is null or ${table.parentCompanyId}<>${table.id}`),
    check("companies_postal_address_check", sql`(${table.street} is null or (char_length(${table.street}) between 1 and 255 and ${table.street}=btrim(${table.street}) and ${table.street} !~ '[[:cntrl:]]')) and (${table.city} is null or (char_length(${table.city}) between 1 and 100 and ${table.city}=btrim(${table.city}) and ${table.city} !~ '[[:cntrl:]]')) and (${table.stateProvince} is null or (char_length(${table.stateProvince}) between 1 and 100 and ${table.stateProvince}=btrim(${table.stateProvince}) and ${table.stateProvince} !~ '[[:cntrl:]]')) and (${table.postalCode} is null or (char_length(${table.postalCode}) between 1 and 30 and ${table.postalCode}=btrim(${table.postalCode}) and ${table.postalCode} !~ '[[:cntrl:]]')) and (${table.country} is null or (char_length(${table.country}) between 1 and 100 and ${table.country}=btrim(${table.country}) and ${table.country} !~ '[[:cntrl:]]'))`),
    check("companies_status_check", sql`${table.status} in ('active','archived')`),
    check("companies_version_check", sql`${table.version} > 0`),
    check("companies_visibility_check", sql`${table.visibility} in ('workspace','teams')`),
    check("companies_authority_contract_check", sql`${table.authorityContractVersion} in ('legacy-p1a-root-v1','customer-graph-v1')`),
    check("companies_archive_metadata_check", sql`(${table.archivedAt} is null)=(${table.archivedByMembershipId} is null) and (${table.status}='archived' or (${table.archivedAt} is null and ${table.archivedByMembershipId} is null)) and (${table.authorityContractVersion}='legacy-p1a-root-v1' or (${table.status}='active' and ${table.archivedAt} is null) or (${table.status}='archived' and ${table.archivedAt} is not null))`),
  ],
);

export const contacts = pgTable(
  "contacts",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    displayName: text("display_name").notNull(),
    personNameNormalized: text("person_name_normalized").notNull(),
    firstName: text("first_name"),
    lastName: text("last_name"),
    salutation: text("salutation"),
    jobTitle: text("job_title"),
    department: text("department"),
    linkedinUrl: text("linkedin_url"),
    lifecycleStage: text("lifecycle_stage"),
    street: text("street"),
    city: text("city"),
    stateProvince: text("state_province"),
    postalCode: text("postal_code"),
    country: text("country"),
    emailDisplay: text("email_display"),
    emailNormalized: text("email_normalized"),
    phoneDisplay: text("phone_display"),
    phoneNormalized: text("phone_normalized"),
    phoneCountryCodeUsed: text("phone_country_code_used"),
    normalizationVersion: text("normalization_version").notNull().default("p1a-identity-v1"),
    companyId: uuid("company_id"),
    status: text("status").notNull().default("active"),
    version: integer("version").notNull().default(1),
    responsibleMembershipId: uuid("responsible_membership_id"),
    responsibleTeamId: uuid("responsible_team_id"),
    visibility: text("visibility").notNull().default("workspace"),
    governingOperationId: uuid("governing_operation_id"),
    createdByMembershipId: uuid("created_by_membership_id"),
    updatedByMembershipId: uuid("updated_by_membership_id"),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    authorityContractVersion: text("authority_contract_version").notNull().default("legacy-p1a-root-v1"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("contacts_workspace_id_id_uq").on(table.workspaceId, table.id),
    index("contacts_workspace_email_idx").on(table.workspaceId, table.emailNormalized, table.id),
    index("contacts_workspace_phone_idx").on(table.workspaceId, table.phoneNormalized, table.id),
    index("contacts_workspace_name_company_idx").on(table.workspaceId, table.personNameNormalized, table.companyId, table.id),
    index("contacts_default_list_idx").on(table.workspaceId, table.status, table.updatedAt.desc().nullsLast(), table.id.desc().nullsLast()),
    index("contacts_responsible_membership_idx").on(table.workspaceId, table.responsibleMembershipId, table.status, table.updatedAt.desc().nullsLast(), table.id.desc().nullsLast()).where(sql`${table.responsibleMembershipId} is not null`),
    index("contacts_responsible_team_idx").on(table.workspaceId, table.responsibleTeamId, table.status, table.updatedAt.desc().nullsLast(), table.id.desc().nullsLast()).where(sql`${table.responsibleTeamId} is not null`),
    foreignKey({ name: "contacts_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
    foreignKey({ name: "contacts_responsible_membership_fk", columns: [table.workspaceId, table.responsibleMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "contacts_responsible_team_fk", columns: [table.workspaceId, table.responsibleTeamId], foreignColumns: [teams.workspaceId, teams.id] }),
    foreignKey({ name: "contacts_creator_membership_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "contacts_updater_membership_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "contacts_archiver_membership_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("contacts_name_check", sql`length(btrim(${table.displayName})) between 1 and 200`),
    check("contacts_profile_fields_check", sql`(${table.salutation} is null or (char_length(${table.salutation}) between 1 and 20 and ${table.salutation}=btrim(${table.salutation}) and ${table.salutation} !~ '[[:cntrl:]]')) and (${table.jobTitle} is null or (char_length(${table.jobTitle}) between 1 and 160 and ${table.jobTitle}=btrim(${table.jobTitle}) and ${table.jobTitle} !~ '[[:cntrl:]]')) and (${table.department} is null or (char_length(${table.department}) between 1 and 120 and ${table.department}=btrim(${table.department}) and ${table.department} !~ '[[:cntrl:]]')) and (${table.linkedinUrl} is null or (char_length(${table.linkedinUrl}) between 1 and 500 and ${table.linkedinUrl}=btrim(${table.linkedinUrl}) and ${table.linkedinUrl} !~ '[[:cntrl:]]'))`),
    check("contacts_lifecycle_stage_check", sql`${table.lifecycleStage} is null or ${table.lifecycleStage} in ('lead','marketing_qualified','sales_qualified','opportunity','customer','evangelist','other')`),
    check("contacts_postal_address_check", sql`(${table.street} is null or (char_length(${table.street}) between 1 and 255 and ${table.street}=btrim(${table.street}) and ${table.street} !~ '[[:cntrl:]]')) and (${table.city} is null or (char_length(${table.city}) between 1 and 100 and ${table.city}=btrim(${table.city}) and ${table.city} !~ '[[:cntrl:]]')) and (${table.stateProvince} is null or (char_length(${table.stateProvince}) between 1 and 100 and ${table.stateProvince}=btrim(${table.stateProvince}) and ${table.stateProvince} !~ '[[:cntrl:]]')) and (${table.postalCode} is null or (char_length(${table.postalCode}) between 1 and 30 and ${table.postalCode}=btrim(${table.postalCode}) and ${table.postalCode} !~ '[[:cntrl:]]')) and (${table.country} is null or (char_length(${table.country}) between 1 and 100 and ${table.country}=btrim(${table.country}) and ${table.country} !~ '[[:cntrl:]]'))`),
    check("contacts_normalized_name_check", sql`length(${table.personNameNormalized}) between 1 and 200 and ${table.personNameNormalized}=btrim(${table.personNameNormalized})`),
    check("contacts_email_pair_check", sql`(${table.emailDisplay} is null) = (${table.emailNormalized} is null)`),
    check("contacts_email_check", sql`${table.emailNormalized} is null or (length(${table.emailNormalized}) between 3 and 320 and ${table.emailNormalized}=lower(btrim(${table.emailNormalized})))`),
    check("contacts_phone_pair_check", sql`(${table.phoneDisplay} is null and ${table.phoneNormalized} is null and ${table.phoneCountryCodeUsed} is null) or (${table.phoneDisplay} is not null and ${table.phoneNormalized} is not null and length(btrim(${table.phoneCountryCodeUsed})) between 2 and 16)`),
    check("contacts_identity_check", sql`${table.emailNormalized} is not null or ${table.phoneNormalized} is not null or length(btrim(${table.displayName})) > 0`),
    check("contacts_status_check", sql`${table.status} in ('active','archived')`),
    check("contacts_version_check", sql`${table.version} > 0`),
    check("contacts_visibility_check", sql`${table.visibility} in ('workspace','teams')`),
    check("contacts_authority_contract_check", sql`${table.authorityContractVersion} in ('legacy-p1a-root-v1','customer-graph-v1')`),
    check("contacts_archive_metadata_check", sql`(${table.archivedAt} is null)=(${table.archivedByMembershipId} is null) and (${table.status}='archived' or (${table.archivedAt} is null and ${table.archivedByMembershipId} is null)) and (${table.authorityContractVersion}='legacy-p1a-root-v1' or (${table.status}='active' and ${table.archivedAt} is null) or (${table.status}='archived' and ${table.archivedAt} is not null))`),
  ],
);

export const contactIdentityPoints = pgTable(
  "contact_identity_points",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    contactId: uuid("contact_id").notNull(),
    kind: text("kind").notNull(),
    channelUsage: text("channel_usage"),
    displayValue: text("display_value").notNull(),
    normalizedValue: text("normalized_value").notNull(),
    phoneCountryCodeUsed: text("phone_country_code_used"),
    normalizationVersion: text("normalization_version").notNull(),
    isPrimary: boolean("is_primary").notNull().default(false),
    verificationStatus: text("verification_status").notNull().default("unverified"),
    verificationMethod: text("verification_method"),
    verifiedAt: timestamp("verified_at", { withTimezone: true }),
    source: text("source").notNull(),
    sourceRecordId: uuid("source_record_id"),
    lifecycle: text("lifecycle").notNull().default("active"),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id"),
    updatedByMembershipId: uuid("updated_by_membership_id"),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("contact_identity_points_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("contact_identity_points_active_value_uq").on(table.workspaceId, table.contactId, table.kind, table.normalizedValue, table.normalizationVersion).where(sql`${table.lifecycle}='active'`),
    uniqueIndex("contact_identity_points_active_primary_uq").on(table.workspaceId, table.contactId, table.kind).where(sql`${table.lifecycle}='active' and ${table.isPrimary}`),
    uniqueIndex("contact_identity_points_active_usage_uq").on(table.workspaceId, table.contactId, table.channelUsage).where(sql`${table.lifecycle}='active' and ${table.channelUsage} is not null`),
    index("contact_identity_points_candidate_idx").on(table.workspaceId, table.kind, table.normalizedValue, table.normalizationVersion, table.lifecycle, table.contactId, table.id),
    index("contact_identity_points_owner_idx").on(table.workspaceId, table.contactId, table.kind, table.lifecycle, table.isPrimary.desc(), table.id),
    foreignKey({ name: "contact_identity_points_contact_fk", columns: [table.workspaceId, table.contactId], foreignColumns: [contacts.workspaceId, contacts.id] }),
    foreignKey({ name: "contact_identity_points_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "contact_identity_points_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "contact_identity_points_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("contact_identity_points_kind_check", sql`${table.kind} in ('email','phone')`),
    check("contact_identity_points_usage_check", sql`${table.channelUsage} is null or ((${table.kind}='email' and ${table.channelUsage} in ('email_primary','email_secondary')) or (${table.kind}='phone' and ${table.channelUsage} in ('phone_primary','phone_direct','phone_mobile'))) and (${table.channelUsage} in ('email_primary','phone_primary'))=${table.isPrimary}`),
    check("contact_identity_points_value_shape_check", sql`(
      ${table.kind}='email' and char_length(${table.displayValue}) between 3 and 320 and char_length(${table.normalizedValue}) between 3 and 320
        and ${table.displayValue}=btrim(${table.displayValue}) and ${table.normalizedValue}=lower(btrim(${table.normalizedValue}))
        and ${table.displayValue} !~ '[[:cntrl:]]' and ${table.normalizedValue} !~ '[[:cntrl:]]' and ${table.phoneCountryCodeUsed} is null
      ) or (
      ${table.kind}='phone' and char_length(${table.displayValue}) between 1 and 50 and char_length(${table.normalizedValue}) between 3 and 32
        and ${table.displayValue}=btrim(${table.displayValue}) and ${table.normalizedValue}=btrim(${table.normalizedValue})
        and ${table.displayValue} !~ '[[:cntrl:]]' and ${table.normalizedValue} !~ '[[:cntrl:]]'
        and ${table.phoneCountryCodeUsed}=btrim(${table.phoneCountryCodeUsed}) and char_length(${table.phoneCountryCodeUsed}) between 2 and 16
        and ${table.phoneCountryCodeUsed} !~ '[[:cntrl:]]'
      )`),
    check("contact_identity_points_normalization_check", sql`${table.normalizationVersion}=btrim(${table.normalizationVersion}) and char_length(${table.normalizationVersion}) between 1 and 64 and ${table.normalizationVersion} !~ '[[:cntrl:]]'`),
    check("contact_identity_points_verification_check", sql`(${table.verificationStatus}='unverified' and ${table.verificationMethod} is null and ${table.verifiedAt} is null) or (${table.verificationStatus}='verified' and ${table.verificationMethod} in ('identity_review','provider','workspace_asserted') and ${table.verifiedAt} is not null)`),
    check("contact_identity_points_source_check", sql`${table.source} in ('legacy_root','lead_identity_review','manual','import','integration') and ((${table.source} in ('lead_identity_review','import','integration') and ${table.sourceRecordId} is not null) or (${table.source} in ('legacy_root','manual') and ${table.sourceRecordId} is null))`),
    check("contact_identity_points_lifecycle_check", sql`${table.lifecycle} in ('active','archived') and ${table.version}>0 and ((${table.lifecycle}='active' and ${table.archivedAt} is null and ${table.archivedByMembershipId} is null) or (${table.lifecycle}='archived' and not ${table.isPrimary} and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null))`),
  ],
);

export const companyDomainPoints = pgTable(
  "company_domain_points",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    companyId: uuid("company_id").notNull(),
    domainDisplay: text("domain_display").notNull(),
    domainNormalized: text("domain_normalized").notNull(),
    normalizationVersion: text("normalization_version").notNull(),
    isPrimary: boolean("is_primary").notNull().default(false),
    verificationStatus: text("verification_status").notNull().default("unverified"),
    verificationMethod: text("verification_method"),
    verifiedAt: timestamp("verified_at", { withTimezone: true }),
    source: text("source").notNull(),
    sourceRecordId: uuid("source_record_id"),
    lifecycle: text("lifecycle").notNull().default("active"),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id"),
    updatedByMembershipId: uuid("updated_by_membership_id"),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("company_domain_points_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("company_domain_points_active_value_uq").on(table.workspaceId, table.companyId, table.domainNormalized, table.normalizationVersion).where(sql`${table.lifecycle}='active'`),
    uniqueIndex("company_domain_points_active_primary_uq").on(table.workspaceId, table.companyId).where(sql`${table.lifecycle}='active' and ${table.isPrimary}`),
    index("company_domain_points_candidate_idx").on(table.workspaceId, table.domainNormalized, table.normalizationVersion, table.lifecycle, table.companyId, table.id),
    index("company_domain_points_owner_idx").on(table.workspaceId, table.companyId, table.lifecycle, table.isPrimary.desc(), table.id),
    foreignKey({ name: "company_domain_points_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
    foreignKey({ name: "company_domain_points_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "company_domain_points_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "company_domain_points_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("company_domain_points_value_check", sql`${table.domainDisplay}=btrim(${table.domainDisplay}) and char_length(${table.domainDisplay}) between 1 and 253 and ${table.domainDisplay} !~ '[[:cntrl:]]' and ${table.domainNormalized}=lower(btrim(${table.domainNormalized})) and char_length(${table.domainNormalized}) between 1 and 253 and ${table.domainNormalized} !~ '[[:cntrl:]]' and ${table.domainNormalized} ~ '^([a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)([.][a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)*$'`),
    check("company_domain_points_normalization_check", sql`${table.normalizationVersion}=btrim(${table.normalizationVersion}) and char_length(${table.normalizationVersion}) between 1 and 64 and ${table.normalizationVersion} !~ '[[:cntrl:]]'`),
    check("company_domain_points_verification_check", sql`(${table.verificationStatus}='unverified' and ${table.verificationMethod} is null and ${table.verifiedAt} is null) or (${table.verificationStatus}='verified' and ${table.verificationMethod} in ('identity_review','provider','workspace_asserted') and ${table.verifiedAt} is not null)`),
    check("company_domain_points_source_check", sql`${table.source} in ('legacy_root','lead_identity_review','manual','import','integration') and ((${table.source} in ('lead_identity_review','import','integration') and ${table.sourceRecordId} is not null) or (${table.source} in ('legacy_root','manual') and ${table.sourceRecordId} is null))`),
    check("company_domain_points_lifecycle_check", sql`${table.lifecycle} in ('active','archived') and ${table.version}>0 and ((${table.lifecycle}='active' and ${table.archivedAt} is null and ${table.archivedByMembershipId} is null) or (${table.lifecycle}='archived' and not ${table.isPrimary} and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null))`),
  ],
);

export const contactCompanyAffiliations = pgTable(
  "contact_company_affiliations",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    contactId: uuid("contact_id").notNull(),
    companyId: uuid("company_id").notNull(),
    roleCode: text("role_code").notNull(),
    isPrimary: boolean("is_primary").notNull().default(false),
    validFrom: timestamp("valid_from", { withTimezone: true }).notNull(),
    validTo: timestamp("valid_to", { withTimezone: true }),
    lifecycle: text("lifecycle").notNull().default("active"),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id"),
    endedByMembershipId: uuid("ended_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("contact_company_affiliations_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("contact_company_affiliations_active_pair_uq").on(table.workspaceId, table.contactId, table.companyId).where(sql`${table.lifecycle}='active'`),
    uniqueIndex("contact_company_affiliations_active_primary_uq").on(table.workspaceId, table.contactId).where(sql`${table.lifecycle}='active' and ${table.isPrimary}`),
    index("contact_company_affiliations_contact_idx").on(table.workspaceId, table.contactId, table.lifecycle, table.isPrimary.desc(), table.companyId, table.id),
    index("contact_company_affiliations_company_idx").on(table.workspaceId, table.companyId, table.lifecycle, table.contactId, table.id),
    index("contact_company_affiliations_history_idx").on(table.workspaceId, table.contactId, table.validFrom.desc(), table.id.desc()),
    foreignKey({ name: "contact_company_affiliations_contact_fk", columns: [table.workspaceId, table.contactId], foreignColumns: [contacts.workspaceId, contacts.id] }),
    foreignKey({ name: "contact_company_affiliations_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
    foreignKey({ name: "contact_company_affiliations_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "contact_company_affiliations_ender_fk", columns: [table.workspaceId, table.endedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("contact_company_affiliations_role_check", sql`${table.roleCode} in ('employee','owner','executive','decision_maker','billing','technical','advisor','contractor','other')`),
    check("contact_company_affiliations_state_check", sql`${table.lifecycle} in ('active','ended') and ${table.version}>0 and ((${table.lifecycle}='active' and ${table.validTo} is null and ${table.endedByMembershipId} is null) or (${table.lifecycle}='ended' and ${table.validTo} is not null and ${table.validTo}>=${table.validFrom} and ${table.endedByMembershipId} is not null))`),
  ],
);

export const contactVisibleTeams = pgTable(
  "contact_visible_teams",
  {
    workspaceId: uuid("workspace_id").notNull(),
    contactId: uuid("contact_id").notNull(),
    teamId: uuid("team_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "contact_visible_teams_pk", columns: [table.workspaceId, table.contactId, table.teamId] }),
    index("contact_visible_teams_team_idx").on(table.workspaceId, table.teamId, table.contactId),
    foreignKey({ name: "contact_visible_teams_contact_fk", columns: [table.workspaceId, table.contactId], foreignColumns: [contacts.workspaceId, contacts.id] }),
    foreignKey({ name: "contact_visible_teams_team_fk", columns: [table.workspaceId, table.teamId], foreignColumns: [teams.workspaceId, teams.id] }),
    foreignKey({ name: "contact_visible_teams_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
  ],
);

export const companyVisibleTeams = pgTable(
  "company_visible_teams",
  {
    workspaceId: uuid("workspace_id").notNull(),
    companyId: uuid("company_id").notNull(),
    teamId: uuid("team_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "company_visible_teams_pk", columns: [table.workspaceId, table.companyId, table.teamId] }),
    index("company_visible_teams_team_idx").on(table.workspaceId, table.teamId, table.companyId),
    foreignKey({ name: "company_visible_teams_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
    foreignKey({ name: "company_visible_teams_team_fk", columns: [table.workspaceId, table.teamId], foreignColumns: [teams.workspaceId, teams.id] }),
    foreignKey({ name: "company_visible_teams_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
  ],
);

export const customerGraphReconciliationRuns = pgTable(
  "customer_graph_reconciliation_runs",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    contractVersion: text("contract_version").notNull().default("customer-graph-reconciliation.v1"),
    state: text("state").notNull().default("pending"),
    sourceCutoff: timestamp("source_cutoff", { withTimezone: true }).notNull(),
    sourceCutoffId: uuid("source_cutoff_id").notNull(),
    counts: jsonb("counts").notNull().default({
      contactsScanned: 0,
      companiesScanned: 0,
      contactEmailPointsWritten: 0,
      contactPhonePointsWritten: 0,
      companyDomainPointsWritten: 0,
      affiliationsWritten: 0,
      issuesOpened: 0,
      issuesResolved: 0,
    }),
    operationId: uuid("operation_id").notNull(),
    startedAt: timestamp("started_at", { withTimezone: true }),
    completedAt: timestamp("completed_at", { withTimezone: true }),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("customer_graph_reconciliation_runs_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("customer_graph_reconciliation_runs_running_uq").on(table.workspaceId).where(sql`${table.state}='running'`),
    index("customer_graph_reconciliation_runs_state_idx").on(table.workspaceId, table.state, table.updatedAt.desc().nullsLast(), table.id.desc().nullsLast()),
    foreignKey({ name: "customer_graph_reconciliation_runs_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("customer_graph_reconciliation_runs_contract_check", sql`${table.contractVersion}='customer-graph-reconciliation.v1'`),
    check("customer_graph_reconciliation_runs_state_check", sql`${table.state} in ('pending','running','blocked','complete','abandoned') and ${table.version}>0 and ((${table.state}='pending' and ${table.startedAt} is null and ${table.completedAt} is null) or (${table.state} in ('running','blocked') and ${table.startedAt} is not null and ${table.completedAt} is null) or (${table.state}='complete' and ${table.startedAt} is not null and ${table.completedAt} is not null) or (${table.state}='abandoned' and ${table.completedAt} is not null))`),
    check("customer_graph_reconciliation_runs_counts_check", sql`jsonb_typeof(${table.counts})='object' and octet_length(${table.counts}::text)<=1024 and ${table.counts} ?& array['contactsScanned','companiesScanned','contactEmailPointsWritten','contactPhonePointsWritten','companyDomainPointsWritten','affiliationsWritten','issuesOpened','issuesResolved'] and (${table.counts}-array['contactsScanned','companiesScanned','contactEmailPointsWritten','contactPhonePointsWritten','companyDomainPointsWritten','affiliationsWritten','issuesOpened','issuesResolved'])='{}'::jsonb
      and jsonb_typeof(${table.counts}->'contactsScanned')='number' and (${table.counts}->>'contactsScanned') ~ '^[0-9]+$' and (${table.counts}->>'contactsScanned')::numeric<=9007199254740991
      and jsonb_typeof(${table.counts}->'companiesScanned')='number' and (${table.counts}->>'companiesScanned') ~ '^[0-9]+$' and (${table.counts}->>'companiesScanned')::numeric<=9007199254740991
      and jsonb_typeof(${table.counts}->'contactEmailPointsWritten')='number' and (${table.counts}->>'contactEmailPointsWritten') ~ '^[0-9]+$' and (${table.counts}->>'contactEmailPointsWritten')::numeric<=9007199254740991
      and jsonb_typeof(${table.counts}->'contactPhonePointsWritten')='number' and (${table.counts}->>'contactPhonePointsWritten') ~ '^[0-9]+$' and (${table.counts}->>'contactPhonePointsWritten')::numeric<=9007199254740991
      and jsonb_typeof(${table.counts}->'companyDomainPointsWritten')='number' and (${table.counts}->>'companyDomainPointsWritten') ~ '^[0-9]+$' and (${table.counts}->>'companyDomainPointsWritten')::numeric<=9007199254740991
      and jsonb_typeof(${table.counts}->'affiliationsWritten')='number' and (${table.counts}->>'affiliationsWritten') ~ '^[0-9]+$' and (${table.counts}->>'affiliationsWritten')::numeric<=9007199254740991
      and jsonb_typeof(${table.counts}->'issuesOpened')='number' and (${table.counts}->>'issuesOpened') ~ '^[0-9]+$' and (${table.counts}->>'issuesOpened')::numeric<=9007199254740991
      and jsonb_typeof(${table.counts}->'issuesResolved')='number' and (${table.counts}->>'issuesResolved') ~ '^[0-9]+$' and (${table.counts}->>'issuesResolved')::numeric<=9007199254740991`),
  ],
);

export const customerGraphReconciliationCheckpoints = pgTable(
  "customer_graph_reconciliation_checkpoints",
  {
    workspaceId: uuid("workspace_id").notNull(),
    runId: uuid("run_id").notNull(),
    stream: text("stream").notNull(),
    lastUpdatedAt: timestamp("last_updated_at", { withTimezone: true }),
    lastId: uuid("last_id"),
    processedCount: bigint("processed_count", { mode: "number" }).notNull().default(0),
    issueCount: bigint("issue_count", { mode: "number" }).notNull().default(0),
    version: integer("version").notNull().default(1),
    updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "customer_graph_reconciliation_checkpoints_pk", columns: [table.workspaceId, table.runId, table.stream] }),
    foreignKey({ name: "customer_graph_reconciliation_checkpoints_run_fk", columns: [table.workspaceId, table.runId], foreignColumns: [customerGraphReconciliationRuns.workspaceId, customerGraphReconciliationRuns.id] }),
    check("customer_graph_reconciliation_checkpoints_stream_check", sql`${table.stream} in ('contact_email','contact_phone','contact_company','company_domain','root_authority')`),
    check("customer_graph_reconciliation_checkpoints_cursor_check", sql`(${table.lastUpdatedAt} is null)=(${table.lastId} is null)`),
    check("customer_graph_reconciliation_checkpoints_counts_check", sql`${table.processedCount}>=0 and ${table.issueCount}>=0 and ${table.version}>0`),
  ],
);

export const customerGraphReconciliationIssues = pgTable(
  "customer_graph_reconciliation_issues",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    runId: uuid("run_id").notNull(),
    stream: text("stream").notNull(),
    sourceRecordType: text("source_record_type").notNull(),
    sourceRecordId: uuid("source_record_id").notNull(),
    issueCode: text("issue_code").notNull(),
    safeMetadata: jsonb("safe_metadata").notNull(),
    state: text("state").notNull().default("open"),
    resolutionCode: text("resolution_code"),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("customer_graph_reconciliation_issues_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("customer_graph_reconciliation_issues_identity_uq").on(table.workspaceId, table.runId, table.stream, table.sourceRecordId, table.issueCode),
    index("customer_graph_reconciliation_issues_lookup_idx").on(table.workspaceId, table.runId, table.state, table.stream, table.sourceRecordId, table.id),
    foreignKey({ name: "customer_graph_reconciliation_issues_run_fk", columns: [table.workspaceId, table.runId], foreignColumns: [customerGraphReconciliationRuns.workspaceId, customerGraphReconciliationRuns.id] }),
    check("customer_graph_reconciliation_issues_stream_check", sql`${table.stream} in ('contact_email','contact_phone','contact_company','company_domain','root_authority')`),
    check("customer_graph_reconciliation_issues_source_check", sql`${table.sourceRecordType} in ('contact','company')`),
    check("customer_graph_reconciliation_issues_code_check", sql`${table.issueCode} in ('missing_normalized_value','invalid_legacy_value','ambiguous_primary','missing_company','version_changed','authority_conflict')`),
    check("customer_graph_reconciliation_issues_state_check", sql`${table.state} in ('open','resolved','waived') and ${table.version}>0 and ((${table.state}='open' and ${table.resolutionCode} is null) or (${table.state}='resolved' and ${table.resolutionCode} in ('source_corrected','accepted_legacy','not_applicable','superseded')) or (${table.state}='waived' and ${table.resolutionCode}='operator_waiver'))`),
    check("customer_graph_reconciliation_issues_metadata_check", sql`jsonb_typeof(${table.safeMetadata})='object' and octet_length(${table.safeMetadata}::text)<=1024 and (
      (${table.issueCode} in ('missing_normalized_value','missing_company') and ${table.safeMetadata} ?& array['sourceVersion'] and (${table.safeMetadata}-array['sourceVersion'])='{}'::jsonb and jsonb_typeof(${table.safeMetadata}->'sourceVersion')='number' and (${table.safeMetadata}->>'sourceVersion') ~ '^[1-9][0-9]*$' and (${table.safeMetadata}->>'sourceVersion')::numeric<=9007199254740991)
      or (${table.issueCode}='invalid_legacy_value' and ${table.safeMetadata} ?& array['sourceVersion','validationCode'] and (${table.safeMetadata}-array['sourceVersion','validationCode'])='{}'::jsonb and jsonb_typeof(${table.safeMetadata}->'sourceVersion')='number' and (${table.safeMetadata}->>'sourceVersion') ~ '^[1-9][0-9]*$' and (${table.safeMetadata}->>'sourceVersion')::numeric<=9007199254740991 and ${table.safeMetadata}->>'validationCode' in ('email_format','phone_format','domain_format','normalization_mismatch'))
      or (${table.issueCode}='ambiguous_primary' and ${table.safeMetadata} ?& array['sourceVersion','activeCandidateCount'] and (${table.safeMetadata}-array['sourceVersion','activeCandidateCount'])='{}'::jsonb and jsonb_typeof(${table.safeMetadata}->'sourceVersion')='number' and (${table.safeMetadata}->>'sourceVersion') ~ '^[1-9][0-9]*$' and (${table.safeMetadata}->>'sourceVersion')::numeric<=9007199254740991 and jsonb_typeof(${table.safeMetadata}->'activeCandidateCount')='number' and (${table.safeMetadata}->>'activeCandidateCount') ~ '^[0-9]+$' and (${table.safeMetadata}->>'activeCandidateCount')::numeric between 2 and 20)
      or (${table.issueCode}='version_changed' and ${table.safeMetadata} ?& array['expectedVersion','observedVersion'] and (${table.safeMetadata}-array['expectedVersion','observedVersion'])='{}'::jsonb and jsonb_typeof(${table.safeMetadata}->'expectedVersion')='number' and (${table.safeMetadata}->>'expectedVersion') ~ '^[1-9][0-9]*$' and (${table.safeMetadata}->>'expectedVersion')::numeric<=9007199254740991 and jsonb_typeof(${table.safeMetadata}->'observedVersion')='number' and (${table.safeMetadata}->>'observedVersion') ~ '^[1-9][0-9]*$' and (${table.safeMetadata}->>'observedVersion')::numeric<=9007199254740991)
      or (${table.issueCode}='authority_conflict' and ${table.safeMetadata} ?& array['sourceVersion','authorityContractVersion'] and (${table.safeMetadata}-array['sourceVersion','authorityContractVersion'])='{}'::jsonb and jsonb_typeof(${table.safeMetadata}->'sourceVersion')='number' and (${table.safeMetadata}->>'sourceVersion') ~ '^[1-9][0-9]*$' and (${table.safeMetadata}->>'sourceVersion')::numeric<=9007199254740991 and ${table.safeMetadata}->>'authorityContractVersion' in ('legacy-p1a-root-v1','customer-graph-v1'))
    )`),
  ],
);

export const leads = pgTable(
  "leads",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    displayName: text("display_name").notNull(),
    personNameNormalized: text("person_name_normalized").notNull(),
    firstName: text("first_name"),
    lastName: text("last_name"),
    salutation: text("salutation"),
    jobTitle: text("job_title"),
    emailNormalized: text("email_normalized"),
    emailDisplay: text("email_display"),
    secondaryEmailNormalized: text("secondary_email_normalized"),
    secondaryEmailDisplay: text("secondary_email_display"),
    company: text("company"),
    phone: text("phone"), // preserved original/display value
    phoneNormalized: text("phone_normalized"),
    phoneCountryCodeUsed: text("phone_country_code_used"),
    mobilePhoneDisplay: text("mobile_phone_display"),
    mobilePhoneNormalized: text("mobile_phone_normalized"),
    mobilePhoneCountryCodeUsed: text("mobile_phone_country_code_used"),
    faxDisplay: text("fax_display"),
    faxNormalized: text("fax_normalized"),
    faxCountryCodeUsed: text("fax_country_code_used"),
    websiteUrl: text("website_url"),
    twitterHandle: text("twitter_handle"),
    promotionalEmailOptOut: boolean("promotional_email_opt_out"),
    promotionalEmailOptOutRecordedAt: timestamp("promotional_email_opt_out_recorded_at", { withTimezone: true }),
    promotionalEmailOptOutSource: text("promotional_email_opt_out_source"),
    rating: text("rating"),
    industry: text("industry"),
    annualRevenueMinor: numeric("annual_revenue_minor", { precision: 20, scale: 0 }),
    annualRevenueCurrencyCode: char("annual_revenue_currency_code", { length: 3 }),
    annualRevenueCurrencyExponent: integer("annual_revenue_currency_exponent"),
    employeeCount: integer("employee_count"),
    street: text("street"),
    city: text("city"),
    stateProvince: text("state_province"),
    postalCode: text("postal_code"),
    country: text("country"),
    normalizationVersion: text("normalization_version").notNull().default("p1a-identity-v1"),
    source: text("source").notNull(),
    originalSourceCategory: text("original_source_category").notNull(),
    originalSourcePlatform: text("original_source_platform"),
    originalSourceMedium: text("original_source_medium").notNull().default("unknown"),
    originalSourceDetail: jsonb("original_source_detail").notNull().default({}),
    originalCampaignContext: jsonb("original_campaign_context").notNull().default({}),
    attributionContractVersion: text("attribution_contract_version").notNull().default("p1a-attribution-v1"),
    intakeChannel: text("intake_channel").notNull().default("manual"),
    receivedAt: timestamp("received_at", { withTimezone: true }).defaultNow().notNull(),
    status: text("status").notNull().default("open"),
    lifecycleDefinitionId: uuid("lifecycle_definition_id").references(() => leadLifecycleDefinitions.id),
    identityReviewStatus: text("identity_review_status").notNull().default("not_required"),
    contactId: uuid("contact_id"),
    companyId: uuid("company_id"),
    stageId: uuid("stage_id").notNull(),
    ownerMembershipId: uuid("owner_membership_id"),
    responsibleTeamId: uuid("responsible_team_id"),
    visibility: text("visibility").notNull().default("workspace"),
    authorityContractVersion: text("authority_contract_version").notNull().default("p1a-lead-v1"),
    governingOperationId: uuid("governing_operation_id"),
    createdByMembershipId: uuid("created_by_membership_id"),
    updatedByMembershipId: uuid("updated_by_membership_id"),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("leads_workspace_id_id_uq").on(table.workspaceId, table.id),
    index("leads_workspace_updated_idx").on(table.workspaceId, table.updatedAt, table.id),
    index("leads_workspace_email_idx").on(table.workspaceId, table.emailNormalized),
    index("leads_workspace_phone_idx").on(table.workspaceId, table.phoneNormalized, table.id),
    index("leads_workspace_name_company_idx").on(table.workspaceId, table.personNameNormalized, table.companyId, table.id),
    index("leads_workspace_lifecycle_idx").on(table.workspaceId, table.lifecycleDefinitionId, table.updatedAt, table.id),
    index("leads_workspace_review_idx").on(table.workspaceId, table.identityReviewStatus, table.updatedAt, table.id),
    foreignKey({ name: "leads_workspace_stage_fk", columns: [table.workspaceId, table.stageId], foreignColumns: [pipelineStages.workspaceId, pipelineStages.id] }),
    foreignKey({ name: "leads_workspace_owner_fk", columns: [table.workspaceId, table.ownerMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "leads_workspace_responsible_team_fk", columns: [table.workspaceId, table.responsibleTeamId], foreignColumns: [teams.workspaceId, teams.id] }),
    foreignKey({ name: "leads_workspace_contact_fk", columns: [table.workspaceId, table.contactId], foreignColumns: [contacts.workspaceId, contacts.id] }),
    foreignKey({ name: "leads_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
    foreignKey({ name: "leads_workspace_created_by_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "leads_workspace_updated_by_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("leads_name_check", sql`length(btrim(${table.displayName})) between 1 and 200 and (${table.firstName} is null or length(btrim(${table.firstName})) between 1 and 100) and (${table.lastName} is null or length(btrim(${table.lastName})) between 1 and 100)`),
    check("leads_normalized_name_check", sql`length(${table.personNameNormalized}) between 1 and 200 and ${table.personNameNormalized}=btrim(${table.personNameNormalized})`),
    check("leads_email_pair_check", sql`(${table.emailDisplay} is null) = (${table.emailNormalized} is null)`),
    check("leads_email_check", sql`${table.emailNormalized} is null or (length(${table.emailNormalized}) between 3 and 320 and ${table.emailNormalized}=lower(btrim(${table.emailNormalized})))`),
    check("leads_secondary_email_pair_check", sql`(${table.secondaryEmailDisplay} is null)=(${table.secondaryEmailNormalized} is null)`),
    check("leads_secondary_email_check", sql`${table.secondaryEmailNormalized} is null or (char_length(${table.secondaryEmailDisplay}) between 3 and 320 and ${table.secondaryEmailDisplay}=btrim(${table.secondaryEmailDisplay}) and ${table.secondaryEmailDisplay} !~ '[[:cntrl:]]' and char_length(${table.secondaryEmailNormalized}) between 3 and 320 and ${table.secondaryEmailNormalized}=lower(btrim(${table.secondaryEmailNormalized})) and ${table.secondaryEmailNormalized} !~ '[[:cntrl:]]')`),
    check("leads_identity_check", sql`${table.emailNormalized} is not null or ${table.phoneNormalized} is not null`),
    check("leads_phone_pair_check", sql`(${table.phone} is null and ${table.phoneNormalized} is null and ${table.phoneCountryCodeUsed} is null) or (${table.lifecycleDefinitionId} is null and ${table.phone} is not null and ${table.phoneNormalized} is null and ${table.phoneCountryCodeUsed} is null) or (${table.phone} is not null and ${table.phoneNormalized} is not null and length(btrim(${table.phoneCountryCodeUsed})) between 2 and 16)`),
    check("leads_mobile_phone_check", sql`(${table.mobilePhoneDisplay} is null and ${table.mobilePhoneNormalized} is null and ${table.mobilePhoneCountryCodeUsed} is null) or (${table.mobilePhoneDisplay} is not null and char_length(${table.mobilePhoneDisplay}) between 1 and 50 and ${table.mobilePhoneDisplay}=btrim(${table.mobilePhoneDisplay}) and ${table.mobilePhoneDisplay} !~ '[[:cntrl:]]' and ${table.mobilePhoneNormalized} is not null and char_length(${table.mobilePhoneNormalized}) between 3 and 32 and ${table.mobilePhoneNormalized}=btrim(${table.mobilePhoneNormalized}) and ${table.mobilePhoneNormalized} !~ '[[:cntrl:]]' and char_length(btrim(${table.mobilePhoneCountryCodeUsed})) between 2 and 16 and ${table.mobilePhoneCountryCodeUsed} !~ '[[:cntrl:]]')`),
    check("leads_fax_check", sql`(${table.faxDisplay} is null and ${table.faxNormalized} is null and ${table.faxCountryCodeUsed} is null) or (${table.faxDisplay} is not null and char_length(${table.faxDisplay}) between 1 and 50 and ${table.faxDisplay}=btrim(${table.faxDisplay}) and ${table.faxDisplay} !~ '[[:cntrl:]]' and ${table.faxNormalized} is not null and char_length(${table.faxNormalized}) between 3 and 32 and ${table.faxNormalized}=btrim(${table.faxNormalized}) and ${table.faxNormalized} !~ '[[:cntrl:]]' and char_length(btrim(${table.faxCountryCodeUsed})) between 2 and 16 and ${table.faxCountryCodeUsed} !~ '[[:cntrl:]]')`),
    check("leads_profile_fields_check", sql`(${table.salutation} is null or (char_length(${table.salutation}) between 1 and 20 and ${table.salutation}=btrim(${table.salutation}) and ${table.salutation} !~ '[[:cntrl:]]')) and (${table.jobTitle} is null or (char_length(${table.jobTitle}) between 1 and 160 and ${table.jobTitle}=btrim(${table.jobTitle}) and ${table.jobTitle} !~ '[[:cntrl:]]')) and (${table.websiteUrl} is null or (char_length(${table.websiteUrl}) between 1 and 500 and ${table.websiteUrl}=btrim(${table.websiteUrl}) and ${table.websiteUrl} !~ '[[:cntrl:]]')) and (${table.twitterHandle} is null or (char_length(${table.twitterHandle}) between 1 and 50 and ${table.twitterHandle}=btrim(${table.twitterHandle}) and ${table.twitterHandle} !~ '[[:cntrl:]]')) and (${table.industry} is null or (char_length(${table.industry}) between 1 and 120 and ${table.industry}=btrim(${table.industry}) and ${table.industry} !~ '[[:cntrl:]]'))`),
    check("leads_consent_fact_check", sql`num_nonnulls(${table.promotionalEmailOptOut},${table.promotionalEmailOptOutRecordedAt},${table.promotionalEmailOptOutSource})=0 or (num_nonnulls(${table.promotionalEmailOptOut},${table.promotionalEmailOptOutRecordedAt},${table.promotionalEmailOptOutSource})=3 and ${table.promotionalEmailOptOutSource} in ('manual','import','integration'))`),
    check("leads_rating_check", sql`${table.rating} is null or ${table.rating} in ('hot','warm','cold')`),
    check("leads_annual_revenue_check", sql`num_nonnulls(${table.annualRevenueMinor},${table.annualRevenueCurrencyCode},${table.annualRevenueCurrencyExponent})=0 or (num_nonnulls(${table.annualRevenueMinor},${table.annualRevenueCurrencyCode},${table.annualRevenueCurrencyExponent})=3 and ${table.annualRevenueMinor} between 0 and 99999999999999999999 and ${table.annualRevenueCurrencyCode} in ('USD','CAD') and ${table.annualRevenueCurrencyExponent}=2)`),
    check("leads_employee_count_check", sql`${table.employeeCount} is null or ${table.employeeCount} between 0 and 2147483647`),
    check("leads_postal_address_check", sql`(${table.street} is null or (char_length(${table.street}) between 1 and 255 and ${table.street}=btrim(${table.street}) and ${table.street} !~ '[[:cntrl:]]')) and (${table.city} is null or (char_length(${table.city}) between 1 and 100 and ${table.city}=btrim(${table.city}) and ${table.city} !~ '[[:cntrl:]]')) and (${table.stateProvince} is null or (char_length(${table.stateProvince}) between 1 and 100 and ${table.stateProvince}=btrim(${table.stateProvince}) and ${table.stateProvince} !~ '[[:cntrl:]]')) and (${table.postalCode} is null or (char_length(${table.postalCode}) between 1 and 30 and ${table.postalCode}=btrim(${table.postalCode}) and ${table.postalCode} !~ '[[:cntrl:]]')) and (${table.country} is null or (char_length(${table.country}) between 1 and 100 and ${table.country}=btrim(${table.country}) and ${table.country} !~ '[[:cntrl:]]'))`),
    check("leads_company_check", sql`${table.company} is null or length(btrim(${table.company})) between 1 and 160`),
    check("leads_source_check", sql`${table.source} in ('website','referral','outbound','event','partner','social_media','import','manual','other')`),
    check("leads_original_source_check", sql`${table.originalSourceCategory} in ('website','referral','outbound','event','partner','social_media','import','manual','other')`),
    check("leads_social_platform_check", sql`coalesce((${table.originalSourceCategory}='social_media' and ${table.originalSourcePlatform} in ('tiktok','instagram','facebook','linkedin','x','youtube','other_social') and (${table.originalSourcePlatform}<>'other_social' or length(btrim(coalesce(${table.originalSourceDetail}->>'platform_context',''))) between 1 and 200)) or (${table.originalSourceCategory}<>'social_media' and ${table.originalSourcePlatform} is null),false)`),
    check("leads_source_medium_check", sql`${table.originalSourceMedium} in ('organic','paid','unknown')`),
    check("leads_source_detail_check", sql`jsonb_typeof(${table.originalSourceDetail})='object' and octet_length(${table.originalSourceDetail}::text)<=2048`),
    check("leads_campaign_context_check", sql`jsonb_typeof(${table.originalCampaignContext})='object' and octet_length(${table.originalCampaignContext}::text)<=2048`),
    check("leads_intake_channel_check", sql`${table.intakeChannel} in ('web_form','manual','csv','spreadsheet','future_api','future_integration')`),
    check("leads_status_check", sql`${table.status} in ('open', 'won', 'lost')`),
    check("leads_review_status_check", sql`${table.identityReviewStatus} in ('not_required','pending','resolved')`),
    check("leads_visibility_check", sql`${table.visibility} in ('workspace', 'teams')`),
    check("leads_authority_contract_check", sql`${table.authorityContractVersion} in ('p1a-lead-v1','lead-vnext-v1') and (${table.authorityContractVersion}='p1a-lead-v1' or ${table.governingOperationId} is not null)`),
    check("leads_version_check", sql`${table.version} > 0`),
  ],
);

export const leadVnextReconciliationRuns = pgTable(
  "lead_vnext_reconciliation_runs",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    contractVersion: text("contract_version").notNull().default("lead-vnext-reconciliation.v1"),
    state: text("state").notNull().default("pending"),
    sourceCutoffAt: timestamp("source_cutoff_at", { withTimezone: true }).notNull(),
    sourceCutoffId: uuid("source_cutoff_id").notNull(),
    leadsScanned: bigint("leads_scanned", { mode: "number" }).notNull().default(0),
    leadsVerified: bigint("leads_verified", { mode: "number" }).notNull().default(0),
    leadsStale: bigint("leads_stale", { mode: "number" }).notNull().default(0),
    leadsBlocked: bigint("leads_blocked", { mode: "number" }).notNull().default(0),
    issuesOpened: bigint("issues_opened", { mode: "number" }).notNull().default(0),
    issuesResolved: bigint("issues_resolved", { mode: "number" }).notNull().default(0),
    operationId: uuid("operation_id").notNull(),
    startedAt: timestamp("started_at", { withTimezone: true }),
    completedAt: timestamp("completed_at", { withTimezone: true }),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("lead_vnext_reconciliation_runs_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("lead_vnext_reconciliation_runs_running_uq").on(table.workspaceId).where(sql`${table.state}='running'`),
    index("lead_vnext_reconciliation_runs_state_idx").on(table.workspaceId, table.state, table.updatedAt.desc().nullsLast(), table.id.desc().nullsLast()),
    foreignKey({ name: "lead_vnext_reconciliation_runs_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("lead_vnext_reconciliation_runs_contract_check", sql`${table.contractVersion}='lead-vnext-reconciliation.v1'`),
    check("lead_vnext_reconciliation_runs_state_check", sql`${table.state} in ('pending','running','blocked','complete','abandoned') and ((${table.state}='pending' and ${table.startedAt} is null and ${table.completedAt} is null) or (${table.state} in ('running','blocked') and ${table.startedAt} is not null and ${table.completedAt} is null) or (${table.state}='complete' and ${table.startedAt} is not null and ${table.completedAt} is not null) or (${table.state}='abandoned' and ${table.completedAt} is not null))`),
    check("lead_vnext_reconciliation_runs_counts_check", sql`${table.leadsScanned} between 0 and 9007199254740991 and ${table.leadsVerified} between 0 and 9007199254740991 and ${table.leadsStale} between 0 and 9007199254740991 and ${table.leadsBlocked} between 0 and 9007199254740991 and ${table.issuesOpened} between 0 and 9007199254740991 and ${table.issuesResolved} between 0 and 9007199254740991 and ${table.leadsVerified}+${table.leadsStale}+${table.leadsBlocked}<=${table.leadsScanned} and ${table.issuesResolved}<=${table.issuesOpened} and (${table.state}<>'pending' or (${table.leadsScanned}=0 and ${table.leadsVerified}=0 and ${table.leadsStale}=0 and ${table.leadsBlocked}=0 and ${table.issuesOpened}=0 and ${table.issuesResolved}=0))`),
    check("lead_vnext_reconciliation_runs_version_check", sql`${table.version}>0`),
  ],
);

export const leadAuthorityStates = pgTable(
  "lead_authority_states",
  {
    workspaceId: uuid("workspace_id").primaryKey().references(() => workspaces.id, { onDelete: "no action" }),
    activeWriter: text("active_writer").notNull().default("p1a"),
    migrationState: text("migration_state").notNull().default("dormant"),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    cutoverRunId: uuid("cutover_run_id"),
    switchedAt: timestamp("switched_at", { withTimezone: true }),
    switchedByMembershipId: uuid("switched_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    foreignKey({ name: "lead_authority_states_cutover_run_fk", columns: [table.workspaceId, table.cutoverRunId], foreignColumns: [leadVnextReconciliationRuns.workspaceId, leadVnextReconciliationRuns.id] }),
    foreignKey({ name: "lead_authority_states_switched_by_fk", columns: [table.workspaceId, table.switchedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("lead_authority_states_state_check", sql`${table.activeWriter} in ('p1a','vnext') and ${table.migrationState} in ('dormant','shadow','reconciling','ready','observing','retirement_ready') and ${table.version}>0 and ((${table.activeWriter}='p1a' and ${table.migrationState} in ('dormant','shadow','reconciling') and ${table.switchedAt} is null and ${table.switchedByMembershipId} is null) or (${table.activeWriter}='p1a' and ${table.migrationState}='ready' and ${table.cutoverRunId} is not null and ${table.switchedAt} is null and ${table.switchedByMembershipId} is null) or (${table.activeWriter}='vnext' and ${table.migrationState} in ('observing','retirement_ready') and ${table.cutoverRunId} is not null and ${table.switchedAt} is not null and ${table.switchedByMembershipId} is not null))`),
  ],
);

export const leadVnextMappings = pgTable(
  "lead_vnext_mappings",
  {
    workspaceId: uuid("workspace_id").notNull(),
    leadId: uuid("lead_id").notNull(),
    sourceContractVersion: text("source_contract_version").notNull().default("p1a-lead-v1"),
    targetContractVersion: text("target_contract_version").notNull().default("lead-vnext-v1"),
    sourceVersion: integer("source_version").notNull(),
    verifiedSourceVersion: integer("verified_source_version"),
    state: text("state").notNull().default("pending"),
    reconciliationRunId: uuid("reconciliation_run_id").notNull(),
    issueCount: bigint("issue_count", { mode: "number" }).notNull().default(0),
    verifiedAt: timestamp("verified_at", { withTimezone: true }),
    governingOperationId: uuid("governing_operation_id").notNull(),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    primaryKey({ name: "lead_vnext_mappings_pk", columns: [table.workspaceId, table.leadId] }),
    index("lead_vnext_mappings_state_idx").on(table.workspaceId, table.state, table.leadId),
    foreignKey({ name: "lead_vnext_mappings_lead_fk", columns: [table.workspaceId, table.leadId], foreignColumns: [leads.workspaceId, leads.id] }),
    foreignKey({ name: "lead_vnext_mappings_run_fk", columns: [table.workspaceId, table.reconciliationRunId], foreignColumns: [leadVnextReconciliationRuns.workspaceId, leadVnextReconciliationRuns.id] }),
    check("lead_vnext_mappings_contract_check", sql`${table.sourceContractVersion}='p1a-lead-v1' and ${table.targetContractVersion}='lead-vnext-v1'`),
    check("lead_vnext_mappings_shape_check", sql`${table.sourceVersion}>0 and (${table.verifiedSourceVersion} is null or ${table.verifiedSourceVersion}>0) and ${table.issueCount} between 0 and 9007199254740991 and ${table.version}>0 and ${table.state} in ('pending','verified','stale','blocked') and ((${table.state}='pending' and ${table.verifiedSourceVersion} is null and ${table.verifiedAt} is null and ${table.issueCount}=0) or (${table.state}='verified' and ${table.verifiedSourceVersion}=${table.sourceVersion} and ${table.verifiedAt} is not null and ${table.issueCount}=0) or (${table.state}='stale' and (${table.verifiedSourceVersion} is null or ${table.verifiedSourceVersion}<${table.sourceVersion})) or (${table.state}='blocked' and ${table.issueCount}>0))`),
  ],
);

export const leadVnextReconciliationCheckpoints = pgTable(
  "lead_vnext_reconciliation_checkpoints",
  {
    workspaceId: uuid("workspace_id").notNull(),
    runId: uuid("run_id").notNull(),
    stream: text("stream").notNull(),
    lastSortAt: timestamp("last_sort_at", { withTimezone: true }),
    lastId: uuid("last_id"),
    processedCount: bigint("processed_count", { mode: "number" }).notNull().default(0),
    issueCount: bigint("issue_count", { mode: "number" }).notNull().default(0),
    version: integer("version").notNull().default(1),
    updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "lead_vnext_reconciliation_checkpoints_pk", columns: [table.workspaceId, table.runId, table.stream] }),
    foreignKey({ name: "lead_vnext_reconciliation_checkpoints_run_fk", columns: [table.workspaceId, table.runId], foreignColumns: [leadVnextReconciliationRuns.workspaceId, leadVnextReconciliationRuns.id] }),
    check("lead_vnext_reconciliation_checkpoints_stream_check", sql`${table.stream} in ('lead_root','intake','identity_review','visibility','lead_history','platform_evidence')`),
    check("lead_vnext_reconciliation_checkpoints_cursor_check", sql`(${table.lastSortAt} is null)=(${table.lastId} is null)`),
    check("lead_vnext_reconciliation_checkpoints_counts_check", sql`${table.processedCount} between 0 and 9007199254740991 and ${table.issueCount} between 0 and 9007199254740991 and ${table.version}>0`),
  ],
);

export const leadVnextReconciliationIssues = pgTable(
  "lead_vnext_reconciliation_issues",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    runId: uuid("run_id").notNull(),
    stream: text("stream").notNull(),
    sourceRecordType: text("source_record_type").notNull(),
    sourceRecordId: uuid("source_record_id").notNull(),
    issueCode: text("issue_code").notNull(),
    expectedVersion: integer("expected_version"),
    observedVersion: integer("observed_version"),
    relatedRecordId: uuid("related_record_id"),
    safeCode: text("safe_code"),
    state: text("state").notNull().default("open"),
    resolutionCode: text("resolution_code"),
    resolvedAt: timestamp("resolved_at", { withTimezone: true }),
    resolvedByMembershipId: uuid("resolved_by_membership_id"),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("lead_vnext_reconciliation_issues_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("lead_vnext_reconciliation_issues_identity_uq").on(table.workspaceId, table.runId, table.stream, table.sourceRecordType, table.sourceRecordId, table.issueCode),
    index("lead_vnext_reconciliation_issues_lookup_idx").on(table.workspaceId, table.runId, table.state, table.stream, table.sourceRecordId, table.id),
    foreignKey({ name: "lead_vnext_reconciliation_issues_run_fk", columns: [table.workspaceId, table.runId], foreignColumns: [leadVnextReconciliationRuns.workspaceId, leadVnextReconciliationRuns.id] }),
    foreignKey({ name: "lead_vnext_reconciliation_issues_resolver_fk", columns: [table.workspaceId, table.resolvedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("lead_vnext_reconciliation_issues_stream_check", sql`${table.stream} in ('lead_root','intake','identity_review','visibility','lead_history','platform_evidence')`),
    check("lead_vnext_reconciliation_issues_source_check", sql`${table.sourceRecordType} in ('lead','lead_intake','identity_review','lead_visible_team','lead_activity','audit_event','outbox_message')`),
    check("lead_vnext_reconciliation_issues_code_check", sql`${table.issueCode} in ('missing_intake','multiple_intakes','lifecycle_status_ambiguous','lifecycle_definition_unavailable','stage_unavailable','assignment_unavailable','visibility_invalid','identity_review_lineage_invalid','linked_record_workspace_mismatch','history_gap','evidence_cardinality_mismatch','source_version_changed','authority_conflict','unsupported_legacy_row')`),
    check("lead_vnext_reconciliation_issues_versions_check", sql`(${table.expectedVersion} is null or ${table.expectedVersion}>0) and (${table.observedVersion} is null or ${table.observedVersion}>0) and ${table.version}>0`),
    check("lead_vnext_reconciliation_issues_safe_code_check", sql`${table.safeCode} is null or (${table.safeCode}=btrim(${table.safeCode}) and length(${table.safeCode}) between 1 and 80 and ${table.safeCode} ~ '^[a-z0-9_]+$')`),
    check("lead_vnext_reconciliation_issues_state_check", sql`${table.state} in ('open','resolved','waived') and ((${table.state}='open' and ${table.resolutionCode} is null and ${table.resolvedAt} is null and ${table.resolvedByMembershipId} is null) or (${table.state}='resolved' and ${table.resolutionCode} in ('source_corrected','reconciled','superseded','not_applicable') and ${table.resolvedAt} is not null and ${table.resolvedByMembershipId} is not null) or (${table.state}='waived' and ${table.resolutionCode}='operator_waiver' and ${table.resolvedAt} is not null and ${table.resolvedByMembershipId} is not null))`),
  ],
);

export const leadIntakes = pgTable(
  "lead_intakes",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    operation: text("operation").notNull().default("lead-inquiry-intake.v1"),
    intakeChannel: text("intake_channel").notNull(),
    idempotencyKey: text("idempotency_key").notNull(),
    actorMembershipId: uuid("actor_membership_id"),
    requestHash: text("request_hash").notNull(),
    contractVersion: text("contract_version").notNull(),
    normalizationVersion: text("normalization_version").notNull(),
    attributionContractVersion: text("attribution_contract_version").notNull(),
    sourceCategory: text("source_category").notNull(),
    sourcePlatform: text("source_platform"),
    sourceMedium: text("source_medium").notNull(),
    sourceDetail: jsonb("source_detail").notNull().default({}),
    campaignContext: jsonb("campaign_context").notNull().default({}),
    state: text("state").notNull().default("pending"),
    leadId: uuid("lead_id"),
    outcome: jsonb("outcome"),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("lead_intakes_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("lead_intakes_idempotency_uq").on(table.workspaceId, table.operation, table.intakeChannel, table.idempotencyKey),
    uniqueIndex("lead_intakes_lead_uq").on(table.workspaceId, table.leadId).where(sql`${table.leadId} is not null`),
    index("lead_intakes_workspace_state_idx").on(table.workspaceId, table.state, table.createdAt, table.id),
    foreignKey({ name: "lead_intakes_workspace_lead_fk", columns: [table.workspaceId, table.leadId], foreignColumns: [leads.workspaceId, leads.id] }),
    foreignKey({ name: "lead_intakes_workspace_actor_fk", columns: [table.workspaceId, table.actorMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("lead_intakes_operation_check", sql`${table.operation}='lead-inquiry-intake.v1'`),
    check("lead_intakes_actor_check", sql`${table.intakeChannel} not in ('manual','csv','spreadsheet') or ${table.actorMembershipId} is not null`),
    check("lead_intakes_channel_check", sql`${table.intakeChannel} in ('manual','csv','spreadsheet','web_form','future_api','future_integration')`),
    check("lead_intakes_key_check", sql`length(${table.idempotencyKey}) between 16 and 128 and length(${table.requestHash}) between 32 and 128`),
    check("lead_intakes_source_check", sql`${table.sourceCategory} in ('website','referral','outbound','event','partner','social_media','import','manual','other')`),
    check("lead_intakes_social_platform_check", sql`coalesce((${table.sourceCategory}='social_media' and ${table.sourcePlatform} in ('tiktok','instagram','facebook','linkedin','x','youtube','other_social') and (${table.sourcePlatform}<>'other_social' or length(btrim(coalesce(${table.sourceDetail}->>'platform_context',''))) between 1 and 200)) or (${table.sourceCategory}<>'social_media' and ${table.sourcePlatform} is null),false)`),
    check("lead_intakes_medium_check", sql`${table.sourceMedium} in ('organic','paid','unknown')`),
    check("lead_intakes_detail_check", sql`jsonb_typeof(${table.sourceDetail})='object' and octet_length(${table.sourceDetail}::text)<=2048`),
    check("lead_intakes_campaign_check", sql`jsonb_typeof(${table.campaignContext})='object' and octet_length(${table.campaignContext}::text)<=2048`),
    check("lead_intakes_state_check", sql`${table.state} in ('pending','committed')`),
    check("lead_intakes_outcome_check", sql`(${table.state}='pending' and ${table.leadId} is null and ${table.outcome} is null) or (${table.state}='committed' and ${table.leadId} is not null and jsonb_typeof(${table.outcome})='object')`),
    check("lead_intakes_version_check", sql`${table.version} > 0`),
  ],
);

export const leadIdentityReviews = pgTable(
  "lead_identity_reviews",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    intakeId: uuid("intake_id").notNull(),
    leadId: uuid("lead_id").notNull(),
    state: text("state").notNull().default("pending"),
    version: integer("version").notNull().default(1),
    resolvedAt: timestamp("resolved_at", { withTimezone: true }),
    resolvedByMembershipId: uuid("resolved_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("lead_identity_reviews_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("lead_identity_reviews_pending_lead_uq").on(table.workspaceId, table.leadId).where(sql`${table.state}='pending'`),
    index("lead_identity_reviews_workspace_state_idx").on(table.workspaceId, table.state, table.updatedAt, table.id),
    foreignKey({ name: "lead_identity_reviews_workspace_intake_fk", columns: [table.workspaceId, table.intakeId], foreignColumns: [leadIntakes.workspaceId, leadIntakes.id] }),
    foreignKey({ name: "lead_identity_reviews_workspace_lead_fk", columns: [table.workspaceId, table.leadId], foreignColumns: [leads.workspaceId, leads.id] }),
    foreignKey({ name: "lead_identity_reviews_workspace_resolver_fk", columns: [table.workspaceId, table.resolvedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("lead_identity_reviews_state_check", sql`${table.state} in ('pending','resolved')`),
    check("lead_identity_reviews_resolution_check", sql`(${table.state}='pending' and ${table.resolvedAt} is null and ${table.resolvedByMembershipId} is null) or (${table.state}='resolved' and ${table.resolvedAt} is not null and ${table.resolvedByMembershipId} is not null)`),
    check("lead_identity_reviews_version_check", sql`${table.version} > 0`),
  ],
);

export const leadIdentityCandidates = pgTable(
  "lead_identity_candidates",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    reviewId: uuid("review_id").notNull(),
    contactId: uuid("contact_id"),
    companyId: uuid("company_id"),
    evidenceKind: text("evidence_kind").notNull(),
    evidenceStrength: text("evidence_strength").notNull(),
    normalizationVersion: text("normalization_version").notNull(),
    targetVersion: integer("target_version").notNull(),
    evidenceMetadata: jsonb("evidence_metadata").notNull().default({}),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    uniqueIndex("lead_identity_candidates_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("lead_identity_candidates_workspace_review_id_uq").on(table.workspaceId, table.reviewId, table.id),
    uniqueIndex("lead_identity_candidates_contact_uq").on(table.workspaceId, table.reviewId, table.contactId, table.evidenceKind, table.normalizationVersion).where(sql`${table.contactId} is not null`),
    uniqueIndex("lead_identity_candidates_company_uq").on(table.workspaceId, table.reviewId, table.companyId, table.evidenceKind, table.normalizationVersion).where(sql`${table.companyId} is not null`),
    index("lead_identity_candidates_review_idx").on(table.workspaceId, table.reviewId, table.evidenceStrength, table.evidenceKind, table.id),
    foreignKey({ name: "lead_identity_candidates_workspace_review_fk", columns: [table.workspaceId, table.reviewId], foreignColumns: [leadIdentityReviews.workspaceId, leadIdentityReviews.id] }),
    foreignKey({ name: "lead_identity_candidates_workspace_contact_fk", columns: [table.workspaceId, table.contactId], foreignColumns: [contacts.workspaceId, contacts.id] }),
    foreignKey({ name: "lead_identity_candidates_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
    check("lead_identity_candidates_target_check", sql`((${table.contactId} is not null)::int + (${table.companyId} is not null)::int)=1`),
    check("lead_identity_candidates_evidence_check", sql`${table.evidenceKind} in ('email','phone','name_company') and ${table.evidenceStrength} in ('strong','supplementary','probable')`),
    check("lead_identity_candidates_strength_check", sql`(${table.evidenceKind}='email' and ${table.evidenceStrength}='strong') or (${table.evidenceKind}='phone' and ${table.evidenceStrength}='supplementary') or (${table.evidenceKind}='name_company' and ${table.evidenceStrength}='probable')`),
    check("lead_identity_candidates_version_check", sql`${table.targetVersion} > 0`),
    check("lead_identity_candidates_metadata_check", sql`jsonb_typeof(${table.evidenceMetadata})='object' and (${table.evidenceMetadata} - array['match_key_version']::text[])='{}'::jsonb`),
  ],
);

export const leadIdentityDecisions = pgTable(
  "lead_identity_decisions",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    intakeId: uuid("intake_id").notNull(),
    reviewId: uuid("review_id").notNull(),
    operation: text("operation").notNull().default("lead-identity-review-decision.v1"),
    idempotencyKey: text("idempotency_key").notNull(),
    requestHash: text("request_hash").notNull(),
    requestId: uuid("request_id").notNull(),
    correlationId: uuid("correlation_id").notNull(),
    supersedesDecisionId: uuid("supersedes_decision_id"),
    governingOutcome: text("governing_outcome").notNull(),
    contactAction: text("contact_action"),
    companyAction: text("company_action"),
    contactId: uuid("contact_id"),
    companyId: uuid("company_id"),
    contactCandidateId: uuid("contact_candidate_id"),
    companyCandidateId: uuid("company_candidate_id"),
    contactTargetVersion: integer("contact_target_version"),
    companyTargetVersion: integer("company_target_version"),
    actorMembershipId: uuid("actor_membership_id").notNull(),
    expectedLeadVersion: integer("expected_lead_version").notNull(),
    expectedReviewVersion: integer("expected_review_version").notNull(),
    expectedIntakeVersion: integer("expected_intake_version").notNull(),
    resultLeadVersion: integer("result_lead_version").notNull(),
    resultReviewVersion: integer("result_review_version").notNull(),
    contractVersion: text("contract_version").notNull(),
    normalizationVersion: text("normalization_version").notNull(),
    reasonCode: text("reason_code"),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    uniqueIndex("lead_identity_decisions_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("lead_identity_decisions_workspace_intake_id_uq").on(table.workspaceId, table.intakeId, table.id),
    uniqueIndex("lead_identity_decisions_idempotency_uq").on(table.workspaceId, table.operation, table.idempotencyKey),
    index("lead_identity_decisions_review_idx").on(table.workspaceId, table.reviewId, table.createdAt, table.id),
    foreignKey({ name: "lead_identity_decisions_workspace_intake_fk", columns: [table.workspaceId, table.intakeId], foreignColumns: [leadIntakes.workspaceId, leadIntakes.id] }),
    foreignKey({ name: "lead_identity_decisions_workspace_review_fk", columns: [table.workspaceId, table.reviewId], foreignColumns: [leadIdentityReviews.workspaceId, leadIdentityReviews.id] }),
    foreignKey({ name: "lead_identity_decisions_workspace_contact_fk", columns: [table.workspaceId, table.contactId], foreignColumns: [contacts.workspaceId, contacts.id] }),
    foreignKey({ name: "lead_identity_decisions_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
    foreignKey({ name: "lead_identity_decisions_workspace_contact_candidate_fk", columns: [table.workspaceId, table.reviewId, table.contactCandidateId], foreignColumns: [leadIdentityCandidates.workspaceId, leadIdentityCandidates.reviewId, leadIdentityCandidates.id] }),
    foreignKey({ name: "lead_identity_decisions_workspace_company_candidate_fk", columns: [table.workspaceId, table.reviewId, table.companyCandidateId], foreignColumns: [leadIdentityCandidates.workspaceId, leadIdentityCandidates.reviewId, leadIdentityCandidates.id] }),
    foreignKey({ name: "lead_identity_decisions_workspace_actor_fk", columns: [table.workspaceId, table.actorMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "lead_identity_decisions_workspace_supersedes_fk", columns: [table.workspaceId, table.intakeId, table.supersedesDecisionId], foreignColumns: [table.workspaceId, table.intakeId, table.id] }),
    check("lead_identity_decisions_operation_check", sql`${table.operation}='lead-identity-review-decision.v1' and length(${table.idempotencyKey}) between 16 and 128 and length(${table.requestHash}) between 32 and 128`),
    check("lead_identity_decisions_outcome_check", sql`${table.governingOutcome} in ('hold','resolve')`),
    check("lead_identity_decisions_actions_check", sql`(${table.governingOutcome}='hold' and num_nonnulls(${table.contactAction},${table.companyAction},${table.contactId},${table.companyId},${table.contactCandidateId},${table.companyCandidateId},${table.contactTargetVersion},${table.companyTargetVersion})=0) or (${table.governingOutcome}='resolve' and ${table.contactAction} in ('create','link','dismiss') and ${table.companyAction} in ('create','link','dismiss') and ((${table.contactAction}='dismiss' and num_nonnulls(${table.contactId},${table.contactCandidateId},${table.contactTargetVersion})=0) or (${table.contactAction}='create' and ${table.contactId} is not null and ${table.contactCandidateId} is null and ${table.contactTargetVersion}>0) or (${table.contactAction}='link' and ${table.contactId} is not null and ${table.contactCandidateId} is not null and ${table.contactTargetVersion}>0)) and ((${table.companyAction}='dismiss' and num_nonnulls(${table.companyId},${table.companyCandidateId},${table.companyTargetVersion})=0) or (${table.companyAction}='create' and ${table.companyId} is not null and ${table.companyCandidateId} is null and ${table.companyTargetVersion}>0) or (${table.companyAction}='link' and ${table.companyId} is not null and ${table.companyCandidateId} is not null and ${table.companyTargetVersion}>0)))`),
    check("lead_identity_decisions_version_check", sql`${table.expectedLeadVersion}>0 and ${table.expectedReviewVersion}>0 and ${table.expectedIntakeVersion}>0 and ${table.resultLeadVersion}>0 and ${table.resultReviewVersion}>0`),
  ],
);

export const leadIdentityDecisionHeads = pgTable(
  "lead_identity_decision_heads",
  {
    workspaceId: uuid("workspace_id").notNull(),
    intakeId: uuid("intake_id").notNull(),
    decisionId: uuid("decision_id").notNull(),
    version: integer("version").notNull().default(1),
    updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "lead_identity_decision_heads_pk", columns: [table.workspaceId, table.intakeId] }),
    uniqueIndex("lead_identity_decision_heads_decision_uq").on(table.workspaceId, table.decisionId),
    foreignKey({ name: "lead_identity_decision_heads_workspace_intake_fk", columns: [table.workspaceId, table.intakeId], foreignColumns: [leadIntakes.workspaceId, leadIntakes.id] }),
    foreignKey({ name: "lead_identity_decision_heads_workspace_decision_fk", columns: [table.workspaceId, table.intakeId, table.decisionId], foreignColumns: [leadIdentityDecisions.workspaceId, leadIdentityDecisions.intakeId, leadIdentityDecisions.id] }),
    check("lead_identity_decision_heads_version_check", sql`${table.version}>0`),
  ],
);

export const leadVisibleTeams = pgTable(
  "lead_visible_teams",
  {
    workspaceId: uuid("workspace_id").notNull(),
    leadId: uuid("lead_id").notNull(),
    teamId: uuid("team_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "lead_visible_teams_pk", columns: [table.leadId, table.teamId] }),
    foreignKey({ name: "lead_visible_teams_lead_fk", columns: [table.workspaceId, table.leadId], foreignColumns: [leads.workspaceId, leads.id] }),
    foreignKey({ name: "lead_visible_teams_team_fk", columns: [table.workspaceId, table.teamId], foreignColumns: [teams.workspaceId, teams.id] }),
  ],
);

export const salesPipelines = pgTable(
  "sales_pipelines",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    code: text("code").notNull(),
    label: text("label").notNull(),
    isDefault: boolean("is_default").notNull().default(false),
    lifecycle: text("lifecycle").notNull().default("active"),
    configurationVersion: integer("configuration_version").notNull().default(1),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    updatedByMembershipId: uuid("updated_by_membership_id").notNull(),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("sales_pipelines_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("sales_pipelines_workspace_code_uq").on(table.workspaceId, table.code),
    uniqueIndex("sales_pipelines_active_default_uq").on(table.workspaceId).where(sql`${table.lifecycle}='active' and ${table.isDefault}`),
    index("sales_pipelines_active_order_idx").on(table.workspaceId, table.lifecycle, table.isDefault, table.label, table.id),
    foreignKey({ name: "sales_pipelines_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "sales_pipelines_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "sales_pipelines_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("sales_pipelines_code_check", sql`${table.code} ~ '^sales\\.[a-z][a-z0-9_]{1,55}$'`),
    check("sales_pipelines_label_check", sql`${table.label}=btrim(${table.label}) and char_length(${table.label}) between 1 and 100 and ${table.label} !~ '[[:cntrl:]]'`),
    check("sales_pipelines_lifecycle_check", sql`${table.lifecycle} in ('active','archived')`),
    check("sales_pipelines_version_check", sql`${table.configurationVersion}>0 and ${table.version}>0`),
    check("sales_pipelines_archive_check", sql`(${table.lifecycle}='active' and ${table.archivedAt} is null and ${table.archivedByMembershipId} is null) or (${table.lifecycle}='archived' and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null and not ${table.isDefault})`),
  ],
);

export const dealStageDefinitions = pgTable(
  "deal_stage_definitions",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    pipelineId: uuid("pipeline_id").notNull(),
    code: text("code").notNull(),
    label: text("label").notNull(),
    outcomeClass: text("outcome_class").notNull(),
    sortKey: bigint("sort_key", { mode: "number" }).notNull(),
    defaultProbabilityBps: integer("default_probability_bps").notNull(),
    lifecycle: text("lifecycle").notNull().default("active"),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    updatedByMembershipId: uuid("updated_by_membership_id").notNull(),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("deal_stage_definitions_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("deal_stage_definitions_stage_pipeline_outcome_uq").on(table.workspaceId, table.id, table.pipelineId, table.outcomeClass),
    uniqueIndex("deal_stage_definitions_pipeline_code_uq").on(table.workspaceId, table.pipelineId, table.code),
    uniqueIndex("deal_stage_definitions_pipeline_sort_uq").on(table.workspaceId, table.pipelineId, table.sortKey),
    index("deal_stage_definitions_active_order_idx").on(table.workspaceId, table.pipelineId, table.lifecycle, table.sortKey, table.id),
    foreignKey({ name: "deal_stage_definitions_pipeline_fk", columns: [table.workspaceId, table.pipelineId], foreignColumns: [salesPipelines.workspaceId, salesPipelines.id] }),
    foreignKey({ name: "deal_stage_definitions_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "deal_stage_definitions_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "deal_stage_definitions_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("deal_stage_definitions_code_check", sql`${table.code} ~ '^sales\\.[a-z][a-z0-9_]{1,55}$'`),
    check("deal_stage_definitions_label_check", sql`${table.label}=btrim(${table.label}) and char_length(${table.label}) between 1 and 100 and ${table.label} !~ '[[:cntrl:]]'`),
    check("deal_stage_definitions_outcome_check", sql`${table.outcomeClass} in ('open','won','lost')`),
    check("deal_stage_definitions_sort_check", sql`${table.sortKey} between 0 and 9007199254740991`),
    check("deal_stage_definitions_probability_check", sql`${table.defaultProbabilityBps} between 0 and 10000 and (${table.outcomeClass}<>'won' or ${table.defaultProbabilityBps}=10000) and (${table.outcomeClass}<>'lost' or ${table.defaultProbabilityBps}=0)`),
    check("deal_stage_definitions_lifecycle_check", sql`${table.lifecycle} in ('active','archived')`),
    check("deal_stage_definitions_version_check", sql`${table.version}>0`),
    check("deal_stage_definitions_archive_check", sql`(${table.lifecycle}='active' and ${table.archivedAt} is null and ${table.archivedByMembershipId} is null) or (${table.lifecycle}='archived' and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null)`),
  ],
);

export const deals = pgTable(
  "deals",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    pipelineId: uuid("pipeline_id").notNull(),
    stageId: uuid("stage_id").notNull(),
    outcomeClass: text("outcome_class").notNull(),
    name: text("name").notNull(),
    lifecycle: text("lifecycle").notNull().default("active"),
    amountMinor: numeric("amount_minor", { precision: 20, scale: 0 }),
    currencyCode: char("currency_code", { length: 3 }),
    currencyExponent: integer("currency_exponent"),
    probabilityBps: integer("probability_bps").notNull(),
    probabilitySource: text("probability_source").notNull().default("stage_default"),
    expectedCloseOn: date("expected_close_on"),
    stageEnteredAt: timestamp("stage_entered_at", { withTimezone: true }).notNull(),
    closedAt: timestamp("closed_at", { withTimezone: true }),
    lostReasonCode: text("lost_reason_code"),
    responsibleMembershipId: uuid("responsible_membership_id").notNull(),
    responsibleTeamId: uuid("responsible_team_id"),
    visibility: text("visibility").notNull().default("workspace"),
    authorityContractVersion: text("authority_contract_version").notNull().default("sales-deal-v1"),
    governingOperationId: uuid("governing_operation_id").notNull(),
    version: integer("version").notNull().default(1),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    updatedByMembershipId: uuid("updated_by_membership_id").notNull(),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("deals_workspace_id_id_uq").on(table.workspaceId, table.id),
    index("deals_default_list_idx").on(table.workspaceId, table.lifecycle, table.updatedAt.desc(), table.id.desc()),
    index("deals_board_stage_idx").on(table.workspaceId, table.pipelineId, table.stageId, table.lifecycle, table.stageEnteredAt, table.id),
    index("deals_responsible_membership_idx").on(table.workspaceId, table.responsibleMembershipId, table.lifecycle, table.updatedAt.desc(), table.id.desc()),
    index("deals_responsible_team_idx").on(table.workspaceId, table.responsibleTeamId, table.lifecycle, table.updatedAt.desc(), table.id.desc()).where(sql`${table.responsibleTeamId} is not null`),
    index("deals_overdue_candidates_idx").on(table.workspaceId, table.outcomeClass, table.expectedCloseOn, table.id).where(sql`${table.lifecycle}='active' and ${table.outcomeClass}='open' and ${table.expectedCloseOn} is not null`),
    foreignKey({ name: "deals_stage_outcome_fk", columns: [table.workspaceId, table.stageId, table.pipelineId, table.outcomeClass], foreignColumns: [dealStageDefinitions.workspaceId, dealStageDefinitions.id, dealStageDefinitions.pipelineId, dealStageDefinitions.outcomeClass] }),
    foreignKey({ name: "deals_responsible_membership_fk", columns: [table.workspaceId, table.responsibleMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "deals_responsible_team_fk", columns: [table.workspaceId, table.responsibleTeamId], foreignColumns: [teams.workspaceId, teams.id] }),
    foreignKey({ name: "deals_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "deals_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "deals_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("deals_name_check", sql`${table.name}=btrim(${table.name}) and char_length(${table.name}) between 1 and 200 and ${table.name} !~ '[[:cntrl:]]'`),
    check("deals_lifecycle_check", sql`${table.lifecycle} in ('active','archived')`),
    check("deals_outcome_check", sql`${table.outcomeClass} in ('open','won','lost')`),
    check("deals_money_check", sql`num_nonnulls(${table.amountMinor},${table.currencyCode},${table.currencyExponent})=0 or (num_nonnulls(${table.amountMinor},${table.currencyCode},${table.currencyExponent})=3 and ${table.amountMinor} between 0 and 99999999999999999999 and (${table.currencyCode},${table.currencyExponent}) in (('USD',2),('CAD',2)))`),
    check("deals_probability_check", sql`${table.probabilityBps} between 0 and 10000 and ${table.probabilitySource} in ('stage_default','manual_override') and (${table.outcomeClass}<>'won' or ${table.probabilityBps}=10000) and (${table.outcomeClass}<>'lost' or ${table.probabilityBps}=0)`),
    check("deals_close_check", sql`(${table.outcomeClass}='open' and ${table.closedAt} is null and ${table.lostReasonCode} is null) or (${table.outcomeClass}='won' and ${table.closedAt} is not null and ${table.lostReasonCode} is null) or (${table.outcomeClass}='lost' and ${table.closedAt} is not null and ${table.lostReasonCode} is not null and ${table.lostReasonCode} in ('budget','timing','no_decision','competitor','needs_mismatch','other'))`),
    check("deals_visibility_check", sql`${table.visibility} in ('workspace','teams')`),
    check("deals_authority_check", sql`${table.authorityContractVersion}='sales-deal-v1' and ${table.version}>0`),
    check("deals_archive_check", sql`(${table.lifecycle}='active' and ${table.archivedAt} is null and ${table.archivedByMembershipId} is null) or (${table.lifecycle}='archived' and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null)`),
  ],
);

export const dealPartyRefs = pgTable(
  "deal_party_refs",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    dealId: uuid("deal_id").notNull(),
    roleCode: text("role_code").notNull(),
    recordType: text("record_type").notNull(),
    recordId: uuid("record_id").notNull(),
    contactSlot: integer("contact_slot"),
    isPrimary: boolean("is_primary").notNull().default(false),
    lifecycle: text("lifecycle").notNull().default("active"),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    endedAt: timestamp("ended_at", { withTimezone: true }),
    endedByMembershipId: uuid("ended_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("deal_party_refs_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("deal_party_refs_active_record_uq").on(table.workspaceId, table.dealId, table.recordType, table.recordId).where(sql`${table.lifecycle}='active'`),
    uniqueIndex("deal_party_refs_active_company_uq").on(table.workspaceId, table.dealId, table.roleCode).where(sql`${table.lifecycle}='active' and ${table.roleCode}='customer_company'`),
    uniqueIndex("deal_party_refs_active_contact_slot_uq").on(table.workspaceId, table.dealId, table.contactSlot).where(sql`${table.lifecycle}='active' and ${table.recordType}='crm.contact'`),
    uniqueIndex("deal_party_refs_active_primary_contact_uq").on(table.workspaceId, table.dealId).where(sql`${table.lifecycle}='active' and ${table.recordType}='crm.contact' and ${table.isPrimary}`),
    index("deal_party_refs_reverse_lookup_idx").on(table.workspaceId, table.recordType, table.recordId, table.lifecycle, table.dealId),
    index("deal_party_refs_deal_idx").on(table.workspaceId, table.dealId, table.lifecycle, table.roleCode, table.contactSlot, table.id),
    foreignKey({ name: "deal_party_refs_deal_fk", columns: [table.workspaceId, table.dealId], foreignColumns: [deals.workspaceId, deals.id] }),
    foreignKey({ name: "deal_party_refs_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "deal_party_refs_ender_fk", columns: [table.workspaceId, table.endedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("deal_party_refs_shape_check", sql`(${table.roleCode}='customer_company' and ${table.recordType}='crm.company' and ${table.contactSlot} is null and not ${table.isPrimary}) or (${table.roleCode}='buying_contact' and ${table.recordType}='crm.contact' and ${table.contactSlot} between 1 and 20)`),
    check("deal_party_refs_lifecycle_check", sql`${table.lifecycle} in ('active','ended') and ${table.version}>0 and ((${table.lifecycle}='active' and ${table.endedAt} is null and ${table.endedByMembershipId} is null) or (${table.lifecycle}='ended' and ${table.endedAt} is not null and ${table.endedByMembershipId} is not null))`),
  ],
);

export const dealVisibleTeams = pgTable(
  "deal_visible_teams",
  {
    workspaceId: uuid("workspace_id").notNull(),
    dealId: uuid("deal_id").notNull(),
    teamId: uuid("team_id").notNull(),
    visibleTeamSlot: integer("visible_team_slot").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "deal_visible_teams_pk", columns: [table.workspaceId, table.dealId, table.teamId] }),
    uniqueIndex("deal_visible_teams_slot_uq").on(table.workspaceId, table.dealId, table.visibleTeamSlot),
    index("deal_visible_teams_team_idx").on(table.workspaceId, table.teamId, table.dealId),
    foreignKey({ name: "deal_visible_teams_deal_fk", columns: [table.workspaceId, table.dealId], foreignColumns: [deals.workspaceId, deals.id] }),
    foreignKey({ name: "deal_visible_teams_team_fk", columns: [table.workspaceId, table.teamId], foreignColumns: [teams.workspaceId, teams.id] }),
    foreignKey({ name: "deal_visible_teams_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("deal_visible_teams_slot_check", sql`${table.visibleTeamSlot} between 1 and 20`),
  ],
);

export const dealStageTransitions = pgTable(
  "deal_stage_transitions",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    dealId: uuid("deal_id").notNull(),
    fromPipelineId: uuid("from_pipeline_id"),
    fromStageId: uuid("from_stage_id"),
    fromOutcomeClass: text("from_outcome_class"),
    toPipelineId: uuid("to_pipeline_id").notNull(),
    toStageId: uuid("to_stage_id").notNull(),
    toOutcomeClass: text("to_outcome_class").notNull(),
    resultDealVersion: integer("result_deal_version").notNull(),
    changedByMembershipId: uuid("changed_by_membership_id").notNull(),
    governingOperationId: uuid("governing_operation_id").notNull(),
    occurredAt: timestamp("occurred_at", { withTimezone: true }).notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    uniqueIndex("deal_stage_transitions_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("deal_stage_transitions_deal_version_uq").on(table.workspaceId, table.dealId, table.resultDealVersion),
    uniqueIndex("deal_stage_transitions_deal_operation_uq").on(table.workspaceId, table.dealId, table.governingOperationId),
    index("deal_stage_transitions_timeline_idx").on(table.workspaceId, table.dealId, table.occurredAt.desc(), table.id.desc()),
    foreignKey({ name: "deal_stage_transitions_deal_fk", columns: [table.workspaceId, table.dealId], foreignColumns: [deals.workspaceId, deals.id] }),
    foreignKey({ name: "deal_stage_transitions_from_stage_fk", columns: [table.workspaceId, table.fromStageId, table.fromPipelineId, table.fromOutcomeClass], foreignColumns: [dealStageDefinitions.workspaceId, dealStageDefinitions.id, dealStageDefinitions.pipelineId, dealStageDefinitions.outcomeClass] }),
    foreignKey({ name: "deal_stage_transitions_to_stage_fk", columns: [table.workspaceId, table.toStageId, table.toPipelineId, table.toOutcomeClass], foreignColumns: [dealStageDefinitions.workspaceId, dealStageDefinitions.id, dealStageDefinitions.pipelineId, dealStageDefinitions.outcomeClass] }),
    foreignKey({ name: "deal_stage_transitions_actor_fk", columns: [table.workspaceId, table.changedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("deal_stage_transitions_from_shape_check", sql`num_nonnulls(${table.fromPipelineId},${table.fromStageId},${table.fromOutcomeClass}) in (0,3)`),
    check("deal_stage_transitions_outcome_check", sql`(${table.fromOutcomeClass} is null or ${table.fromOutcomeClass} in ('open','won','lost')) and ${table.toOutcomeClass} in ('open','won','lost')`),
    check("deal_stage_transitions_version_check", sql`${table.resultDealVersion}>0`),
  ],
);

export const leadDealConversionLineage = pgTable(
  "lead_deal_conversion_lineage",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    leadRecordType: text("lead_record_type").notNull().default("crm.lead"),
    leadRecordId: uuid("lead_record_id").notNull(),
    dealId: uuid("deal_id").notNull(),
    contractVersion: text("contract_version").notNull().default("lead-convert-to-deal.v1"),
    sourceLeadVersion: integer("source_lead_version").notNull(),
    resultLeadVersion: integer("result_lead_version").notNull(),
    resultDealVersion: integer("result_deal_version").notNull(),
    governingOperationId: uuid("governing_operation_id").notNull(),
    convertedByMembershipId: uuid("converted_by_membership_id").notNull(),
    convertedAt: timestamp("converted_at", { withTimezone: true }).notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    uniqueIndex("lead_deal_conversion_lineage_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("lead_deal_conversion_lineage_lead_uq").on(table.workspaceId, table.leadRecordType, table.leadRecordId),
    uniqueIndex("lead_deal_conversion_lineage_deal_uq").on(table.workspaceId, table.dealId),
    uniqueIndex("lead_deal_conversion_lineage_operation_uq").on(table.workspaceId, table.governingOperationId),
    foreignKey({ name: "lead_deal_conversion_lineage_deal_fk", columns: [table.workspaceId, table.dealId], foreignColumns: [deals.workspaceId, deals.id] }),
    foreignKey({ name: "lead_deal_conversion_lineage_actor_fk", columns: [table.workspaceId, table.convertedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("lead_deal_conversion_lineage_contract_check", sql`${table.leadRecordType}='crm.lead' and ${table.contractVersion}='lead-convert-to-deal.v1'`),
    check("lead_deal_conversion_lineage_versions_check", sql`${table.sourceLeadVersion}>0 and ${table.resultLeadVersion}>${table.sourceLeadVersion} and ${table.resultDealVersion}>0`),
  ],
);

export const leadActivities = pgTable(
  "lead_activities",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    leadId: uuid("lead_id").notNull(),
    kind: text("kind").notNull(),
    body: text("body").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    index("lead_activities_lead_idx").on(table.workspaceId, table.leadId, table.createdAt),
    foreignKey({ name: "lead_activities_lead_fk", columns: [table.workspaceId, table.leadId], foreignColumns: [leads.workspaceId, leads.id] }),
    foreignKey({ name: "lead_activities_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("lead_activities_kind_check", sql`${table.kind} in ('note', 'created', 'updated', 'stage_changed', 'status_changed')`),
    check("lead_activities_body_check", sql`length(btrim(${table.body})) between 1 and 4000`),
  ],
);

export const activityRecords = pgTable(
  "activity_records",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    origin: text("origin").notNull().default("manual"),
    kind: text("kind").notNull(),
    direction: text("direction"),
    outcome: text("outcome"),
    occurredAt: timestamp("occurred_at", { withTimezone: true }).notNull(),
    durationMinutes: integer("duration_minutes"),
    subject: text("subject").notNull(),
    details: text("details"),
    version: integer("version").notNull().default(1),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("activity_records_workspace_id_id_uq").on(table.workspaceId, table.id),
    index("activity_records_workspace_timeline_idx").on(table.workspaceId, table.occurredAt.desc(), table.id.desc()),
    index("activity_records_workspace_kind_timeline_idx").on(table.workspaceId, table.kind, table.occurredAt.desc(), table.id.desc()),
    foreignKey({ name: "activity_records_workspace_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("activity_records_origin_check", sql`${table.origin}='manual'`),
    check("activity_records_kind_check", sql`${table.kind} in ('note','call','meeting','email','message','other')`),
    check("activity_records_direction_check", sql`${table.direction} is null or ${table.direction} in ('inbound','outbound','internal')`),
    check("activity_records_outcome_check", sql`${table.outcome} is null or ${table.outcome} in ('completed','connected','no_answer','left_message','rescheduled','cancelled','follow_up_required','other')`),
    check("activity_records_duration_check", sql`${table.durationMinutes} is null or ${table.durationMinutes} between 1 and 1440`),
    check("activity_records_subject_check", sql`length(btrim(${table.subject})) between 1 and 200`),
    check("activity_records_details_check", sql`${table.details} is null or length(btrim(${table.details})) between 1 and 10000`),
    check("activity_records_version_check", sql`${table.version}>0`),
  ],
);

export const activityRecordReferences = pgTable(
  "activity_record_references",
  {
    workspaceId: uuid("workspace_id").notNull(),
    activityId: uuid("activity_id").notNull(),
    recordType: text("record_type").notNull(),
    recordId: uuid("record_id").notNull(),
    occurredAt: timestamp("occurred_at", { withTimezone: true }).notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "activity_record_references_pk", columns: [table.workspaceId, table.activityId] }),
    index("activity_record_references_record_lookup_idx").on(table.workspaceId, table.recordType, table.recordId, table.activityId),
    index("activity_record_references_target_timeline_idx").on(table.workspaceId, table.recordType, table.recordId, table.occurredAt.desc().nullsLast(), table.activityId.desc().nullsLast()),
    foreignKey({ name: "activity_record_references_activity_fk", columns: [table.workspaceId, table.activityId], foreignColumns: [activityRecords.workspaceId, activityRecords.id] }),
    check("activity_record_references_type_check", sql`${table.recordType}='crm.lead'`),
  ],
);

export const noteRecords = pgTable(
  "note_records",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    lifecycle: text("lifecycle").notNull().default("active"),
    version: integer("version").notNull().default(1),
    currentRevisionNumber: integer("current_revision_number").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    updatedByMembershipId: uuid("updated_by_membership_id").notNull(),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("note_records_workspace_id_id_uq").on(table.workspaceId, table.id),
    index("note_records_workspace_lifecycle_updated_idx").on(table.workspaceId, table.lifecycle, table.updatedAt.desc(), table.id.desc()),
    foreignKey({ name: "note_records_workspace_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "note_records_workspace_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "note_records_workspace_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("note_records_lifecycle_check", sql`${table.lifecycle} in ('active','archived')`),
    check("note_records_version_check", sql`${table.version}>0 and ${table.currentRevisionNumber}=${table.version}`),
    check("note_records_lifecycle_metadata_check", sql`(${table.lifecycle}='active' and num_nonnulls(${table.archivedAt},${table.archivedByMembershipId})=0) or (${table.lifecycle}='archived' and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null)`),
  ],
);

export const noteRevisions = pgTable(
  "note_revisions",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    noteId: uuid("note_id").notNull(),
    revisionNumber: integer("revision_number").notNull(),
    subject: text("subject"),
    body: text("body").notNull(),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    uniqueIndex("note_revisions_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("note_revisions_workspace_note_number_uq").on(table.workspaceId, table.noteId, table.revisionNumber),
    foreignKey({ name: "note_revisions_note_fk", columns: [table.workspaceId, table.noteId], foreignColumns: [noteRecords.workspaceId, noteRecords.id] }),
    foreignKey({ name: "note_revisions_workspace_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("note_revisions_number_check", sql`${table.revisionNumber}>0`),
    check("note_revisions_content_check", sql`(${table.subject} is null or char_length(btrim(${table.subject})) between 1 and 200) and char_length(btrim(${table.body})) between 1 and 20000`),
  ],
);

export const noteRecordReferences = pgTable(
  "note_record_references",
  {
    workspaceId: uuid("workspace_id").notNull(),
    noteId: uuid("note_id").notNull(),
    recordType: text("record_type").notNull(),
    recordId: uuid("record_id").notNull(),
    relationshipRole: text("relationship_role").notNull().default("related"),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "note_record_references_pk", columns: [table.workspaceId, table.noteId, table.recordType, table.recordId] }),
    index("note_record_references_target_idx").on(table.workspaceId, table.recordType, table.recordId, table.noteId),
    foreignKey({ name: "note_record_references_note_fk", columns: [table.workspaceId, table.noteId], foreignColumns: [noteRecords.workspaceId, noteRecords.id] }),
    foreignKey({ name: "note_record_references_workspace_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("note_record_references_type_check", sql`${table.recordType} in ('crm.lead','crm.contact','crm.company','sales.deal','delivery.project')`),
    check("note_record_references_role_check", sql`${table.relationshipRole}='related'`),
  ],
);

export const documentRecords = pgTable(
  "document_records",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    lifecycle: text("lifecycle").notNull().default("active"),
    availability: text("availability").notNull().default("awaiting_upload"),
    version: integer("version").notNull().default(1),
    currentContentVersion: integer("current_content_version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    updatedByMembershipId: uuid("updated_by_membership_id").notNull(),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    redactionRequestedAt: timestamp("redaction_requested_at", { withTimezone: true }),
    redactionRequestedByMembershipId: uuid("redaction_requested_by_membership_id"),
    redactedAt: timestamp("redacted_at", { withTimezone: true }),
    redactedByMembershipId: uuid("redacted_by_membership_id"),
    purgeRequestedAt: timestamp("purge_requested_at", { withTimezone: true }),
    purgeRequestedByMembershipId: uuid("purge_requested_by_membership_id"),
    purgedAt: timestamp("purged_at", { withTimezone: true }),
    purgedByMembershipId: uuid("purged_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("document_records_workspace_id_id_uq").on(table.workspaceId, table.id),
    index("document_records_workspace_lifecycle_updated_idx").on(table.workspaceId, table.lifecycle, table.updatedAt.desc(), table.id.desc()),
    index("document_records_workspace_creator_lifecycle_updated_idx").on(table.workspaceId, table.createdByMembershipId, table.lifecycle, table.updatedAt.desc(), table.id.desc()),
    foreignKey({ name: "document_records_workspace_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "document_records_workspace_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "document_records_workspace_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "document_records_workspace_redaction_requester_fk", columns: [table.workspaceId, table.redactionRequestedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "document_records_workspace_redactor_fk", columns: [table.workspaceId, table.redactedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "document_records_workspace_purge_requester_fk", columns: [table.workspaceId, table.purgeRequestedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "document_records_workspace_purger_fk", columns: [table.workspaceId, table.purgedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("document_records_lifecycle_check", sql`${table.lifecycle} in ('active','archived','redaction_pending','redacted','purge_pending','purged')`),
    check("document_records_availability_check", sql`${table.availability} in ('awaiting_upload','quarantined','available','blocked','failed','unavailable')`),
    check("document_records_version_check", sql`${table.version}>0 and ${table.currentContentVersion} between 1 and 100`),
    check("document_records_metadata_pairs_check", sql`num_nonnulls(${table.archivedAt},${table.archivedByMembershipId})<>1 and num_nonnulls(${table.redactionRequestedAt},${table.redactionRequestedByMembershipId})<>1 and num_nonnulls(${table.redactedAt},${table.redactedByMembershipId})<>1 and num_nonnulls(${table.purgeRequestedAt},${table.purgeRequestedByMembershipId})<>1 and num_nonnulls(${table.purgedAt},${table.purgedByMembershipId})<>1`),
    check("document_records_lifecycle_metadata_check", sql`
      (${table.lifecycle}='active' and num_nonnulls(${table.archivedAt},${table.archivedByMembershipId},${table.redactionRequestedAt},${table.redactionRequestedByMembershipId},${table.redactedAt},${table.redactedByMembershipId},${table.purgeRequestedAt},${table.purgeRequestedByMembershipId},${table.purgedAt},${table.purgedByMembershipId})=0) or
      (${table.lifecycle}='archived' and ${table.archivedAt} is not null and ${table.redactionRequestedAt} is null and ${table.redactedAt} is null and ${table.purgeRequestedAt} is null and ${table.purgedAt} is null) or
      (${table.lifecycle}='redaction_pending' and ${table.redactionRequestedAt} is not null and ${table.redactedAt} is null and ${table.purgeRequestedAt} is null and ${table.purgedAt} is null) or
      (${table.lifecycle}='redacted' and ${table.redactionRequestedAt} is not null and ${table.redactedAt} is not null and ${table.purgeRequestedAt} is null and ${table.purgedAt} is null) or
      (${table.lifecycle}='purge_pending' and ${table.purgeRequestedAt} is not null and ${table.purgedAt} is null and (${table.archivedAt} is not null or (${table.redactionRequestedAt} is not null and ${table.redactedAt} is not null))) or
      (${table.lifecycle}='purged' and ${table.purgeRequestedAt} is not null and ${table.purgedAt} is not null and (${table.archivedAt} is not null or (${table.redactionRequestedAt} is not null and ${table.redactedAt} is not null)))`),
    check("document_records_terminal_availability_check", sql`${table.lifecycle} not in ('redaction_pending','redacted','purge_pending','purged') or ${table.availability}='unavailable'`),
  ],
);

export const documentVersions = pgTable(
  "document_versions",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    documentId: uuid("document_id").notNull(),
    contentVersion: integer("content_version").notNull(),
    state: text("state").notNull().default("reserved"),
    displayFilename: text("display_filename"),
    declaredMimeType: text("declared_mime_type"),
    detectedMimeType: text("detected_mime_type"),
    byteSize: bigint("byte_size", { mode: "number" }),
    sha256Hex: char("sha256_hex", { length: 64 }),
    redactionMarker: text("redaction_marker"),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
    availableAt: timestamp("available_at", { withTimezone: true }),
    redactedAt: timestamp("redacted_at", { withTimezone: true }),
    purgedAt: timestamp("purged_at", { withTimezone: true }),
  },
  (table) => [
    uniqueIndex("document_versions_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("document_versions_workspace_document_content_uq").on(table.workspaceId, table.documentId, table.contentVersion),
    index("document_versions_workspace_document_content_idx").on(table.workspaceId, table.documentId, table.contentVersion.desc()),
    foreignKey({ name: "document_versions_document_fk", columns: [table.workspaceId, table.documentId], foreignColumns: [documentRecords.workspaceId, documentRecords.id] }),
    foreignKey({ name: "document_versions_workspace_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("document_versions_content_version_check", sql`${table.contentVersion} between 1 and 100`),
    check("document_versions_state_check", sql`${table.state} in ('reserved','uploaded','quarantined','available','blocked','failed','redacted','purged')`),
    check("document_versions_filename_check", sql`${table.displayFilename} is null or (char_length(btrim(${table.displayFilename})) between 1 and 255 and ${table.displayFilename} !~ '[[:cntrl:]/\\\\]')`),
    check("document_versions_declared_mime_check", sql`${table.declaredMimeType} is null or ${table.declaredMimeType} in ('application/pdf','image/jpeg','image/png','image/webp','text/plain','text/csv','application/vnd.openxmlformats-officedocument.wordprocessingml.document','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','application/vnd.openxmlformats-officedocument.presentationml.presentation')`),
    check("document_versions_detected_mime_check", sql`${table.detectedMimeType} is null or ${table.detectedMimeType} in ('application/pdf','image/jpeg','image/png','image/webp','text/plain','text/csv','application/vnd.openxmlformats-officedocument.wordprocessingml.document','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','application/vnd.openxmlformats-officedocument.presentationml.presentation')`),
    check("document_versions_size_check", sql`${table.byteSize} is null or ${table.byteSize} between 1 and 26214400`),
    check("document_versions_hash_check", sql`${table.sha256Hex} is null or ${table.sha256Hex} ~ '^[0-9a-f]{64}$'`),
    check("document_versions_state_metadata_check", sql`
      (${table.state}='reserved' and ${table.displayFilename} is not null and ${table.declaredMimeType} is not null and ${table.detectedMimeType} is null and ${table.byteSize} is null and ${table.sha256Hex} is null and ${table.availableAt} is null and ${table.redactedAt} is null and ${table.purgedAt} is null and ${table.redactionMarker} is null) or
      (${table.state} in ('uploaded','quarantined') and ${table.displayFilename} is not null and ${table.declaredMimeType} is not null and ${table.detectedMimeType} is not null and ${table.byteSize} is not null and ${table.sha256Hex} is not null and ${table.availableAt} is null and ${table.redactedAt} is null and ${table.purgedAt} is null and ${table.redactionMarker} is null) or
      (${table.state}='available' and ${table.displayFilename} is not null and ${table.declaredMimeType} is not null and ${table.detectedMimeType} is not null and ${table.byteSize} is not null and ${table.sha256Hex} is not null and ${table.availableAt} is not null and ${table.redactedAt} is null and ${table.purgedAt} is null and ${table.redactionMarker} is null) or
      (${table.state} in ('blocked','failed') and ${table.displayFilename} is not null and ${table.declaredMimeType} is not null and ${table.detectedMimeType} is not null and ${table.byteSize} is not null and ${table.sha256Hex} is not null and ${table.redactedAt} is null and ${table.purgedAt} is null and ${table.redactionMarker} is null) or
      (${table.state}='redacted' and ${table.displayFilename} is null and ${table.declaredMimeType} is null and ${table.detectedMimeType} is null and ${table.byteSize} is null and ${table.sha256Hex} is null and ${table.redactionMarker}='content_redacted' and ${table.redactedAt} is not null and ${table.purgedAt} is null) or
      (${table.state}='purged' and ${table.displayFilename} is null and ${table.declaredMimeType} is null and ${table.detectedMimeType} is null and ${table.byteSize} is null and ${table.sha256Hex} is null and ${table.redactionMarker}='content_redacted' and ${table.purgedAt} is not null)`),
  ],
);

export const documentRecordReferences = pgTable(
  "document_record_references",
  {
    workspaceId: uuid("workspace_id").notNull(),
    documentId: uuid("document_id").notNull(),
    recordType: text("record_type").notNull(),
    recordId: uuid("record_id").notNull(),
    relationshipRole: text("relationship_role").notNull().default("related"),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "document_record_references_pk", columns: [table.workspaceId, table.documentId, table.recordType, table.recordId] }),
    index("document_record_references_target_idx").on(table.workspaceId, table.recordType, table.recordId, table.documentId),
    foreignKey({ name: "document_record_references_document_fk", columns: [table.workspaceId, table.documentId], foreignColumns: [documentRecords.workspaceId, documentRecords.id] }),
    foreignKey({ name: "document_record_references_workspace_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("document_record_references_type_check", sql`${table.recordType} in ('crm.lead','crm.contact','crm.company','sales.deal','delivery.project')`),
    check("document_record_references_role_check", sql`${table.relationshipRole}='related'`),
  ],
);

export const documentStorageObjects = pgTable(
  "document_storage_objects",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    documentId: uuid("document_id").notNull(),
    contentVersion: integer("content_version").notNull(),
    storageAdapterCode: text("storage_adapter_code").notNull(),
    residencyRegionCode: text("residency_region_code").notNull(),
    residencyPolicyVersion: text("residency_policy_version").notNull(),
    containerHandle: text("container_handle").notNull(),
    objectKey: text("object_key").notNull(),
    providerObjectVersion: text("provider_object_version"),
    etag: text("etag"),
    encryptionMode: text("encryption_mode").notNull(),
    encryptionKeyHandle: text("encryption_key_handle"),
    state: text("state").notNull().default("reserved"),
    uploadExpiresAt: timestamp("upload_expires_at", { withTimezone: true }).notNull(),
    uploadVerifiedAt: timestamp("upload_verified_at", { withTimezone: true }),
    deleteRequestedAt: timestamp("delete_requested_at", { withTimezone: true }),
    deleteVerifiedAt: timestamp("delete_verified_at", { withTimezone: true }),
    nextAttemptAt: timestamp("next_attempt_at", { withTimezone: true }),
    attemptCount: integer("attempt_count").notNull().default(0),
    governingOperationId: uuid("governing_operation_id").notNull(),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("document_storage_objects_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("document_storage_objects_workspace_document_content_uq").on(table.workspaceId, table.documentId, table.contentVersion),
    uniqueIndex("document_storage_objects_locator_uq").on(table.storageAdapterCode, table.residencyRegionCode, table.containerHandle, table.objectKey),
    index("document_storage_objects_worker_idx").on(table.state, table.nextAttemptAt, table.id),
    index("document_storage_objects_workspace_state_updated_idx").on(table.workspaceId, table.state, table.updatedAt, table.id),
    foreignKey({ name: "document_storage_objects_version_fk", columns: [table.workspaceId, table.documentId, table.contentVersion], foreignColumns: [documentVersions.workspaceId, documentVersions.documentId, documentVersions.contentVersion] }),
    check("document_storage_objects_adapter_check", sql`${table.storageAdapterCode} ~ '^[a-z][a-z0-9_.-]{1,63}$' and ${table.residencyRegionCode} ~ '^[a-z][a-z0-9_.-]{1,63}$'`),
    check("document_storage_objects_policy_check", sql`char_length(btrim(${table.residencyPolicyVersion})) between 1 and 64`),
    check("document_storage_objects_container_check", sql`char_length(btrim(${table.containerHandle})) between 1 and 200 and ${table.containerHandle} !~ '[[:cntrl:]]'`),
    check("document_storage_objects_key_check", sql`char_length(btrim(${table.objectKey})) between 1 and 512 and ${table.objectKey} !~ '[[:cntrl:]]' and ${table.objectKey} !~ '^/' and ${table.objectKey} !~ '(^|/)\\.{1,2}(/|$)'`),
    check("document_storage_objects_provider_facts_check", sql`(${table.providerObjectVersion} is null or (char_length(btrim(${table.providerObjectVersion})) between 1 and 256 and ${table.providerObjectVersion} !~ '[[:cntrl:]]')) and (${table.etag} is null or (char_length(btrim(${table.etag})) between 1 and 256 and ${table.etag} !~ '[[:cntrl:]]'))`),
    check("document_storage_objects_encryption_check", sql`${table.encryptionMode} in ('provider_managed','customer_managed_envelope') and ((${table.state}<>'purged' and ${table.encryptionMode}='provider_managed' and ${table.encryptionKeyHandle} is null) or (${table.state}<>'purged' and ${table.encryptionMode}='customer_managed_envelope' and char_length(btrim(${table.encryptionKeyHandle})) between 1 and 256 and ${table.encryptionKeyHandle} !~ '[[:cntrl:]]') or (${table.state}='purged' and ${table.encryptionKeyHandle} is null))`),
    check("document_storage_objects_state_check", sql`${table.state} in ('reserved','uploaded','quarantined','scanning','clean','blocked','failed','delete_pending','purged')`),
    check("document_storage_objects_attempt_check", sql`${table.attemptCount} between 0 and 3`),
    check("document_storage_objects_state_metadata_check", sql`
      (${table.state}='reserved' and ${table.uploadVerifiedAt} is null and ${table.deleteRequestedAt} is null and ${table.deleteVerifiedAt} is null) or
      (${table.state} in ('uploaded','quarantined','scanning','clean','blocked','failed') and ${table.uploadVerifiedAt} is not null and ${table.deleteRequestedAt} is null and ${table.deleteVerifiedAt} is null) or
      (${table.state}='delete_pending' and ${table.deleteRequestedAt} is not null and ${table.deleteVerifiedAt} is null) or
      (${table.state}='purged' and ${table.deleteRequestedAt} is not null and ${table.deleteVerifiedAt} is not null and ${table.providerObjectVersion} is null and ${table.etag} is null)`),
    check("document_storage_objects_retry_check", sql`${table.nextAttemptAt} is null or ${table.state} in ('quarantined','scanning','failed')`),
  ],
);

export const documentScanResults = pgTable(
  "document_scan_results",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    storageObjectId: uuid("storage_object_id").notNull(),
    attemptNumber: integer("attempt_number").notNull(),
    outcome: text("outcome").notNull(),
    engineCode: text("engine_code").notNull(),
    engineVersion: text("engine_version").notNull(),
    signatureSetVersion: text("signature_set_version").notNull(),
    scannedSha256Hex: char("scanned_sha256_hex", { length: 64 }).notNull(),
    safeResultCode: text("safe_result_code").notNull(),
    startedAt: timestamp("started_at", { withTimezone: true }).notNull(),
    completedAt: timestamp("completed_at", { withTimezone: true }).notNull(),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    uniqueIndex("document_scan_results_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("document_scan_results_workspace_object_attempt_uq").on(table.workspaceId, table.storageObjectId, table.attemptNumber),
    index("document_scan_results_workspace_object_attempt_idx").on(table.workspaceId, table.storageObjectId, table.attemptNumber.desc()),
    foreignKey({ name: "document_scan_results_object_fk", columns: [table.workspaceId, table.storageObjectId], foreignColumns: [documentStorageObjects.workspaceId, documentStorageObjects.id] }),
    check("document_scan_results_attempt_check", sql`${table.attemptNumber} between 1 and 3`),
    check("document_scan_results_outcome_check", sql`${table.outcome} in ('clean','infected','error','timeout')`),
    check("document_scan_results_codes_check", sql`char_length(btrim(${table.engineCode})) between 1 and 128 and ${table.engineCode} !~ '[[:cntrl:]]' and char_length(btrim(${table.engineVersion})) between 1 and 128 and ${table.engineVersion} !~ '[[:cntrl:]]' and char_length(btrim(${table.signatureSetVersion})) between 1 and 128 and ${table.signatureSetVersion} !~ '[[:cntrl:]]' and char_length(btrim(${table.safeResultCode})) between 1 and 128 and ${table.safeResultCode} !~ '[[:cntrl:]]'`),
    check("document_scan_results_hash_check", sql`${table.scannedSha256Hex} ~ '^[0-9a-f]{64}$'`),
    check("document_scan_results_time_check", sql`${table.completedAt}>=${table.startedAt}`),
  ],
);

export const retentionLegalHolds = pgTable(
  "retention_legal_holds",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    recordType: text("record_type").notNull().default("crm.document"),
    recordId: uuid("record_id").notNull(),
    status: text("status").notNull().default("active"),
    reasonCode: text("reason_code").notNull(),
    caseReference: text("case_reference"),
    policyVersion: text("policy_version").notNull(),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    placedAt: timestamp("placed_at", { withTimezone: true }).defaultNow().notNull(),
    placedByMembershipId: uuid("placed_by_membership_id").notNull(),
    releasedAt: timestamp("released_at", { withTimezone: true }),
    releasedByMembershipId: uuid("released_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("retention_legal_holds_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("retention_legal_holds_active_record_uq").on(table.workspaceId, table.recordType, table.recordId).where(sql`${table.status}='active'`),
    index("retention_legal_holds_record_idx").on(table.workspaceId, table.recordType, table.recordId, table.status, table.id),
    index("retention_legal_holds_workspace_status_updated_idx").on(table.workspaceId, table.status, table.updatedAt, table.id),
    foreignKey({ name: "retention_legal_holds_workspace_placer_fk", columns: [table.workspaceId, table.placedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "retention_legal_holds_workspace_releaser_fk", columns: [table.workspaceId, table.releasedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("retention_legal_holds_record_type_check", sql`${table.recordType}='crm.document'`),
    check("retention_legal_holds_status_check", sql`${table.status} in ('active','released')`),
    check("retention_legal_holds_reason_check", sql`${table.reasonCode} in ('legal_dispute','regulatory','investigation','customer_request','other')`),
    check("retention_legal_holds_case_check", sql`${table.caseReference} is null or (char_length(btrim(${table.caseReference})) between 1 and 200 and ${table.caseReference} !~ '[[:cntrl:]]')`),
    check("retention_legal_holds_policy_check", sql`char_length(btrim(${table.policyVersion})) between 1 and 64`),
    check("retention_legal_holds_version_check", sql`${table.version}>0`),
    check("retention_legal_holds_release_check", sql`(${table.status}='active' and ${table.releasedAt} is null and ${table.releasedByMembershipId} is null) or (${table.status}='released' and ${table.releasedAt} is not null and ${table.releasedByMembershipId} is not null)`),
  ],
);

const customFieldDefinitionColumns = {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    targetRecordType: text("target_record_type").notNull(),
    code: text("code").notNull(),
    label: text("label").notNull(),
    description: text("description"),
    fieldType: text("field_type").notNull(),
    lifecycle: text("lifecycle").notNull().default("draft"),
    required: boolean("required").notNull().default(false),
    searchable: boolean("searchable").notNull().default(false),
    filterable: boolean("filterable").notNull().default(false),
    sortable: boolean("sortable").notNull().default(false),
    displayOrder: integer("display_order").notNull(),
    normalizationVersion: text("normalization_version").notNull().default("custom-field-normalization-v1"),
    defaultTextValue: text("default_text_value"),
    defaultIntegerValue: bigint("default_integer_value", { mode: "number" }),
    defaultDecimalValue: numeric("default_decimal_value", { precision: 18, scale: 6 }),
    defaultBooleanValue: boolean("default_boolean_value"),
    defaultDateValue: date("default_date_value"),
    defaultTimestampValue: timestamp("default_timestamp_value", { withTimezone: true }),
    defaultOptionId: uuid("default_option_id"),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    updatedByMembershipId: uuid("updated_by_membership_id").notNull(),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
  ...timestamps,
};
// Drizzle needs an explicit shape to type the mutually referential composite option FKs.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const customFieldDefinitionsShape = () => pgTable("custom_field_definitions", customFieldDefinitionColumns);
export const customFieldDefinitions: ReturnType<typeof customFieldDefinitionsShape> = pgTable(
  "custom_field_definitions",
  customFieldDefinitionColumns,
  (table) => [
    uniqueIndex("custom_field_definitions_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("custom_field_definitions_workspace_target_code_uq").on(table.workspaceId, table.targetRecordType, table.code),
    uniqueIndex("custom_field_definitions_active_order_uq").on(table.workspaceId, table.targetRecordType, table.displayOrder).where(sql`${table.lifecycle}<>'archived'`),
    index("custom_field_definitions_admin_idx").on(table.workspaceId, table.targetRecordType, table.lifecycle, table.displayOrder, table.id),
    foreignKey({ name: "custom_field_definitions_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "custom_field_definitions_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "custom_field_definitions_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "custom_field_definitions_default_option_fk", columns: [table.workspaceId, table.defaultOptionId, table.id], foreignColumns: [customFieldOptions.workspaceId, customFieldOptions.id, customFieldOptions.definitionId] }),
    check("custom_field_definitions_target_check", sql`${table.targetRecordType} in ('crm.lead','crm.contact','crm.company','sales.deal','delivery.project')`),
    check("custom_field_definitions_code_check", sql`${table.code} ~ '^[a-z][a-z0-9_]{1,63}$'`),
    check("custom_field_definitions_label_check", sql`${table.label}=btrim(${table.label}) and char_length(${table.label}) between 1 and 80 and ${table.label} !~ '[[:cntrl:]]'`),
    check("custom_field_definitions_description_check", sql`${table.description} is null or (${table.description}=btrim(${table.description}) and char_length(${table.description}) between 1 and 500 and ${table.description} !~ '[[:cntrl:]]')`),
    check("custom_field_definitions_type_check", sql`${table.fieldType} in ('short_text','long_text','integer','decimal','boolean','date','timestamp','single_select','multi_select')`),
    check("custom_field_definitions_lifecycle_check", sql`${table.lifecycle} in ('draft','active','archived')`),
    check("custom_field_definitions_order_version_check", sql`${table.displayOrder} between 0 and 9999 and ${table.version}>0`),
    check("custom_field_definitions_normalization_check", sql`${table.normalizationVersion}='custom-field-normalization-v1'`),
    check("custom_field_definitions_archive_check", sql`(${table.lifecycle}='archived' and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null) or (${table.lifecycle}<>'archived' and ${table.archivedAt} is null and ${table.archivedByMembershipId} is null)`),
    check("custom_field_definitions_capabilities_check", sql`(not ${table.searchable} or ${table.fieldType} in ('short_text','single_select')) and (not ${table.filterable} or ${table.fieldType} in ('short_text','integer','decimal','boolean','date','timestamp','single_select','multi_select')) and (not ${table.sortable} or ${table.fieldType} in ('short_text','integer','decimal','boolean','date','timestamp','single_select')) and (${table.fieldType}<>'long_text' or (not ${table.searchable} and not ${table.filterable} and not ${table.sortable}))`),
    check("custom_field_definitions_default_count_check", sql`num_nonnulls(${table.defaultTextValue},${table.defaultIntegerValue},${table.defaultDecimalValue},${table.defaultBooleanValue},${table.defaultDateValue},${table.defaultTimestampValue},${table.defaultOptionId})<=1`),
    check("custom_field_definitions_default_shape_check", sql`coalesce((
      (${table.defaultTextValue} is null and ${table.defaultIntegerValue} is null and ${table.defaultDecimalValue} is null and ${table.defaultBooleanValue} is null and ${table.defaultDateValue} is null and ${table.defaultTimestampValue} is null and ${table.defaultOptionId} is null)
      or (${table.fieldType}='short_text' and ${table.defaultTextValue}=btrim(${table.defaultTextValue}) and char_length(${table.defaultTextValue}) between 1 and 500 and ${table.defaultTextValue} !~ '[[:cntrl:]]')
      or (${table.fieldType}='long_text' and ${table.defaultTextValue}=btrim(${table.defaultTextValue}) and char_length(${table.defaultTextValue}) between 1 and 10000 and ${table.defaultTextValue} !~ '[[:cntrl:]]')
      or (${table.fieldType}='integer' and ${table.defaultIntegerValue} between -9007199254740991 and 9007199254740991)
      or (${table.fieldType}='decimal' and ${table.defaultDecimalValue} between -999999999999.999999 and 999999999999.999999)
      or (${table.fieldType}='boolean' and ${table.defaultBooleanValue} is not null)
      or (${table.fieldType}='date' and ${table.defaultDateValue} is not null)
      or (${table.fieldType}='timestamp' and ${table.defaultTimestampValue} is not null)
      or (${table.fieldType}='single_select' and ${table.defaultOptionId} is not null)),false)`),
    check("custom_field_definitions_multi_default_check", sql`${table.fieldType}<>'multi_select' or num_nonnulls(${table.defaultTextValue},${table.defaultIntegerValue},${table.defaultDecimalValue},${table.defaultBooleanValue},${table.defaultDateValue},${table.defaultTimestampValue},${table.defaultOptionId})=0`),
  ],
);

const customFieldOptionColumns = {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    definitionId: uuid("definition_id").notNull(),
    code: text("code").notNull(),
    label: text("label").notNull(),
    displayOrder: integer("display_order").notNull(),
    lifecycle: text("lifecycle").notNull().default("active"),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    updatedByMembershipId: uuid("updated_by_membership_id").notNull(),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
  ...timestamps,
};
// Drizzle needs an explicit shape to type the mutually referential composite definition FKs.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const customFieldOptionsShape = () => pgTable("custom_field_options", customFieldOptionColumns);
export const customFieldOptions: ReturnType<typeof customFieldOptionsShape> = pgTable(
  "custom_field_options",
  customFieldOptionColumns,
  (table) => [
    uniqueIndex("custom_field_options_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("custom_field_options_workspace_id_definition_uq").on(table.workspaceId, table.id, table.definitionId),
    uniqueIndex("custom_field_options_definition_code_uq").on(table.workspaceId, table.definitionId, table.code),
    uniqueIndex("custom_field_options_active_order_uq").on(table.workspaceId, table.definitionId, table.displayOrder).where(sql`${table.lifecycle}='active'`),
    index("custom_field_options_admin_idx").on(table.workspaceId, table.definitionId, table.lifecycle, table.displayOrder, table.id),
    foreignKey({ name: "custom_field_options_definition_fk", columns: [table.workspaceId, table.definitionId], foreignColumns: [customFieldDefinitions.workspaceId, customFieldDefinitions.id] }),
    foreignKey({ name: "custom_field_options_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "custom_field_options_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "custom_field_options_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("custom_field_options_code_check", sql`${table.code} ~ '^[a-z][a-z0-9_]{1,63}$'`),
    check("custom_field_options_label_check", sql`${table.label}=btrim(${table.label}) and char_length(${table.label}) between 1 and 80 and ${table.label} !~ '[[:cntrl:]]'`),
    check("custom_field_options_order_check", sql`${table.displayOrder} between 0 and 999`),
    check("custom_field_options_lifecycle_check", sql`${table.lifecycle} in ('active','archived')`),
    check("custom_field_options_version_check", sql`${table.version}>0`),
    check("custom_field_options_archive_check", sql`(${table.lifecycle}='archived' and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null) or (${table.lifecycle}='active' and ${table.archivedAt} is null and ${table.archivedByMembershipId} is null)`),
  ],
);

export const customFieldValues = pgTable(
  "custom_field_values",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    definitionId: uuid("definition_id").notNull(),
    targetRecordType: text("target_record_type").notNull(),
    targetRecordId: uuid("target_record_id").notNull(),
    fieldType: text("field_type").notNull(),
    lifecycle: text("lifecycle").notNull().default("active"),
    textValue: text("text_value"),
    textNormalized: text("text_normalized"),
    integerValue: bigint("integer_value", { mode: "number" }),
    decimalValue: numeric("decimal_value", { precision: 18, scale: 6 }),
    booleanValue: boolean("boolean_value"),
    dateValue: date("date_value"),
    timestampValue: timestamp("timestamp_value", { withTimezone: true }),
    redactionMarker: text("redaction_marker"),
    normalizationVersion: text("normalization_version").notNull().default("custom-field-normalization-v1"),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    updatedByMembershipId: uuid("updated_by_membership_id").notNull(),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    redactedAt: timestamp("redacted_at", { withTimezone: true }),
    redactedByMembershipId: uuid("redacted_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("custom_field_values_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("custom_field_values_workspace_id_definition_uq").on(table.workspaceId, table.id, table.definitionId),
    uniqueIndex("custom_field_values_target_definition_uq").on(table.workspaceId, table.definitionId, table.targetRecordType, table.targetRecordId),
    index("custom_field_values_text_idx").on(table.workspaceId, table.definitionId, table.textNormalized.asc().op("text_pattern_ops"), table.targetRecordType, table.targetRecordId).where(sql`${table.lifecycle}='active' and ${table.textNormalized} is not null`),
    index("custom_field_values_integer_idx").on(table.workspaceId, table.definitionId, table.integerValue, table.targetRecordType, table.targetRecordId).where(sql`${table.lifecycle}='active' and ${table.integerValue} is not null`),
    index("custom_field_values_decimal_idx").on(table.workspaceId, table.definitionId, table.decimalValue, table.targetRecordType, table.targetRecordId).where(sql`${table.lifecycle}='active' and ${table.decimalValue} is not null`),
    index("custom_field_values_boolean_idx").on(table.workspaceId, table.definitionId, table.booleanValue, table.targetRecordType, table.targetRecordId).where(sql`${table.lifecycle}='active' and ${table.booleanValue} is not null`),
    index("custom_field_values_date_idx").on(table.workspaceId, table.definitionId, table.dateValue, table.targetRecordType, table.targetRecordId).where(sql`${table.lifecycle}='active' and ${table.dateValue} is not null`),
    index("custom_field_values_timestamp_idx").on(table.workspaceId, table.definitionId, table.timestampValue, table.targetRecordType, table.targetRecordId).where(sql`${table.lifecycle}='active' and ${table.timestampValue} is not null`),
    foreignKey({ name: "custom_field_values_definition_fk", columns: [table.workspaceId, table.definitionId], foreignColumns: [customFieldDefinitions.workspaceId, customFieldDefinitions.id] }),
    foreignKey({ name: "custom_field_values_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "custom_field_values_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "custom_field_values_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "custom_field_values_redactor_fk", columns: [table.workspaceId, table.redactedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("custom_field_values_target_check", sql`${table.targetRecordType} in ('crm.lead','crm.contact','crm.company','sales.deal','delivery.project')`),
    check("custom_field_values_type_check", sql`${table.fieldType} in ('short_text','long_text','integer','decimal','boolean','date','timestamp','single_select','multi_select')`),
    check("custom_field_values_lifecycle_check", sql`${table.lifecycle} in ('active','archived','redacted')`),
    check("custom_field_values_version_normalization_check", sql`${table.version}>0 and ${table.normalizationVersion}='custom-field-normalization-v1'`),
    check("custom_field_values_metadata_check", sql`
      (${table.lifecycle}='active' and ${table.archivedAt} is null and ${table.archivedByMembershipId} is null and ${table.redactedAt} is null and ${table.redactedByMembershipId} is null)
      or (${table.lifecycle}='archived' and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null and ${table.redactedAt} is null and ${table.redactedByMembershipId} is null)
      or (${table.lifecycle}='redacted' and ${table.redactedAt} is not null and ${table.redactedByMembershipId} is not null and ((${table.archivedAt} is null and ${table.archivedByMembershipId} is null) or (${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null)))`),
    check("custom_field_values_shape_check", sql`coalesce((
      (${table.lifecycle}='redacted' and num_nonnulls(${table.textValue},${table.textNormalized},${table.integerValue},${table.decimalValue},${table.booleanValue},${table.dateValue},${table.timestampValue})=0 and ${table.redactionMarker}='content_redacted')
      or (${table.lifecycle}<>'redacted' and ${table.redactionMarker} is null and (
        (${table.fieldType}='short_text' and ${table.textValue}=btrim(${table.textValue}) and char_length(${table.textValue}) between 1 and 500 and ${table.textValue} !~ '[[:cntrl:]]' and ${table.textNormalized}=lower(btrim(${table.textNormalized})) and ${table.textNormalized}=lower(btrim(${table.textValue})) and num_nonnulls(${table.textValue},${table.textNormalized})=2 and num_nonnulls(${table.integerValue},${table.decimalValue},${table.booleanValue},${table.dateValue},${table.timestampValue})=0)
        or (${table.fieldType}='long_text' and ${table.textValue}=btrim(${table.textValue}) and char_length(${table.textValue}) between 1 and 10000 and ${table.textValue} !~ '[[:cntrl:]]' and ${table.textNormalized} is null and num_nonnulls(${table.integerValue},${table.decimalValue},${table.booleanValue},${table.dateValue},${table.timestampValue})=0)
        or (${table.fieldType}='integer' and ${table.integerValue} between -9007199254740991 and 9007199254740991 and num_nonnulls(${table.textValue},${table.textNormalized},${table.decimalValue},${table.booleanValue},${table.dateValue},${table.timestampValue})=0)
        or (${table.fieldType}='decimal' and ${table.decimalValue} between -999999999999.999999 and 999999999999.999999 and num_nonnulls(${table.textValue},${table.textNormalized},${table.integerValue},${table.booleanValue},${table.dateValue},${table.timestampValue})=0)
        or (${table.fieldType}='boolean' and ${table.booleanValue} is not null and num_nonnulls(${table.textValue},${table.textNormalized},${table.integerValue},${table.decimalValue},${table.dateValue},${table.timestampValue})=0)
        or (${table.fieldType}='date' and ${table.dateValue} is not null and num_nonnulls(${table.textValue},${table.textNormalized},${table.integerValue},${table.decimalValue},${table.booleanValue},${table.timestampValue})=0)
        or (${table.fieldType}='timestamp' and ${table.timestampValue} is not null and num_nonnulls(${table.textValue},${table.textNormalized},${table.integerValue},${table.decimalValue},${table.booleanValue},${table.dateValue})=0)
        or (${table.fieldType} in ('single_select','multi_select') and num_nonnulls(${table.textValue},${table.textNormalized},${table.integerValue},${table.decimalValue},${table.booleanValue},${table.dateValue},${table.timestampValue})=0)))),false)`),
  ],
);

export const customFieldValueOptions = pgTable(
  "custom_field_value_options",
  {
    workspaceId: uuid("workspace_id").notNull(),
    valueId: uuid("value_id").notNull(),
    definitionId: uuid("definition_id").notNull(),
    optionId: uuid("option_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "custom_field_value_options_pk", columns: [table.workspaceId, table.valueId, table.optionId] }),
    index("custom_field_value_options_lookup_idx").on(table.workspaceId, table.definitionId, table.optionId, table.valueId),
    foreignKey({ name: "custom_field_value_options_value_fk", columns: [table.workspaceId, table.valueId, table.definitionId], foreignColumns: [customFieldValues.workspaceId, customFieldValues.id, customFieldValues.definitionId] }),
    foreignKey({ name: "custom_field_value_options_option_fk", columns: [table.workspaceId, table.optionId, table.definitionId], foreignColumns: [customFieldOptions.workspaceId, customFieldOptions.id, customFieldOptions.definitionId] }),
    foreignKey({ name: "custom_field_value_options_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
  ],
);

export const customizationTags = pgTable(
  "customization_tags",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    code: text("code").notNull(),
    label: text("label").notNull(),
    normalizedLabel: text("normalized_label").notNull(),
    normalizationVersion: text("normalization_version").notNull().default("tag-normalization-v1"),
    colorCode: text("color_code").notNull().default("neutral"),
    lifecycle: text("lifecycle").notNull().default("active"),
    version: integer("version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    updatedByMembershipId: uuid("updated_by_membership_id").notNull(),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("customization_tags_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("customization_tags_workspace_code_uq").on(table.workspaceId, table.code),
    uniqueIndex("customization_tags_workspace_label_uq").on(table.workspaceId, table.normalizedLabel),
    index("customization_tags_admin_idx").on(table.workspaceId, table.lifecycle, table.normalizedLabel, table.id),
    foreignKey({ name: "customization_tags_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "customization_tags_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "customization_tags_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("customization_tags_code_check", sql`${table.code} ~ '^[a-z][a-z0-9_]{1,63}$'`),
    check("customization_tags_label_check", sql`${table.label}=btrim(${table.label}) and char_length(${table.label}) between 1 and 50 and ${table.label} !~ '[[:cntrl:]]' and ${table.normalizedLabel}=lower(btrim(${table.normalizedLabel})) and char_length(${table.normalizedLabel}) between 1 and 50 and ${table.normalizedLabel}=lower(btrim(${table.label}))`),
    check("customization_tags_normalization_check", sql`${table.normalizationVersion}='tag-normalization-v1'`),
    check("customization_tags_color_check", sql`${table.colorCode} in ('neutral','gray','red','orange','amber','green','teal','blue','indigo','violet','pink')`),
    check("customization_tags_lifecycle_check", sql`${table.lifecycle} in ('active','archived') and ${table.version}>0`),
    check("customization_tags_archive_check", sql`(${table.lifecycle}='archived' and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null) or (${table.lifecycle}='active' and ${table.archivedAt} is null and ${table.archivedByMembershipId} is null)`),
  ],
);

export const recordTagAssignments = pgTable(
  "record_tag_assignments",
  {
    workspaceId: uuid("workspace_id").notNull(),
    tagId: uuid("tag_id").notNull(),
    recordType: text("record_type").notNull(),
    recordId: uuid("record_id").notNull(),
    assignedByMembershipId: uuid("assigned_by_membership_id").notNull(),
    assignedAt: timestamp("assigned_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ name: "record_tag_assignments_pk", columns: [table.workspaceId, table.tagId, table.recordType, table.recordId] }),
    index("record_tag_assignments_target_idx").on(table.workspaceId, table.recordType, table.recordId, table.tagId),
    foreignKey({ name: "record_tag_assignments_tag_fk", columns: [table.workspaceId, table.tagId], foreignColumns: [customizationTags.workspaceId, customizationTags.id] }),
    foreignKey({ name: "record_tag_assignments_assigner_fk", columns: [table.workspaceId, table.assignedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("record_tag_assignments_type_check", sql`${table.recordType} in ('crm.lead','crm.contact','crm.company','sales.deal','delivery.project')`),
  ],
);

export const savedLists = pgTable(
  "saved_lists",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "no action" }),
    targetRecordType: text("target_record_type").notNull(),
    name: text("name").notNull(),
    description: text("description"),
    visibility: text("visibility").notNull().default("private"),
    ownerMembershipId: uuid("owner_membership_id").notNull(),
    lifecycle: text("lifecycle").notNull().default("active"),
    version: integer("version").notNull().default(1),
    currentDefinitionVersion: integer("current_definition_version").notNull().default(1),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    updatedByMembershipId: uuid("updated_by_membership_id").notNull(),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    archivedByMembershipId: uuid("archived_by_membership_id"),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("saved_lists_workspace_id_id_uq").on(table.workspaceId, table.id),
    index("saved_lists_owner_discovery_idx").on(table.workspaceId, table.ownerMembershipId, table.lifecycle, table.updatedAt.desc().nullsLast(), table.id.desc().nullsLast()),
    index("saved_lists_workspace_discovery_idx").on(table.workspaceId, table.visibility, table.lifecycle, table.updatedAt.desc().nullsLast(), table.id.desc().nullsLast()),
    foreignKey({ name: "saved_lists_owner_fk", columns: [table.workspaceId, table.ownerMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "saved_lists_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "saved_lists_updater_fk", columns: [table.workspaceId, table.updatedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    foreignKey({ name: "saved_lists_archiver_fk", columns: [table.workspaceId, table.archivedByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("saved_lists_target_check", sql`${table.targetRecordType} in ('crm.lead','crm.contact','crm.company','sales.deal','delivery.project')`),
    check("saved_lists_name_check", sql`${table.name}=btrim(${table.name}) and char_length(${table.name}) between 1 and 100 and ${table.name} !~ '[[:cntrl:]]'`),
    check("saved_lists_description_check", sql`${table.description} is null or (${table.description}=btrim(${table.description}) and char_length(${table.description}) between 1 and 500 and ${table.description} !~ '[[:cntrl:]]')`),
    check("saved_lists_visibility_check", sql`${table.visibility} in ('private','workspace')`),
    check("saved_lists_lifecycle_check", sql`${table.lifecycle} in ('active','archived') and ${table.version}>0 and ${table.currentDefinitionVersion}>0`),
    check("saved_lists_archive_check", sql`(${table.lifecycle}='archived' and ${table.archivedAt} is not null and ${table.archivedByMembershipId} is not null) or (${table.lifecycle}='active' and ${table.archivedAt} is null and ${table.archivedByMembershipId} is null)`),
  ],
);

export const savedListVersions = pgTable(
  "saved_list_versions",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull(),
    listId: uuid("list_id").notNull(),
    definitionVersion: integer("definition_version").notNull(),
    contractVersion: text("contract_version").notNull().default("saved-list-filter.v1"),
    filterAst: jsonb("filter_ast").notNull(),
    filterAstHash: char("filter_ast_hash", { length: 64 }).notNull(),
    sortSource: text("sort_source").notNull(),
    sortFieldCode: text("sort_field_code"),
    sortDefinitionId: uuid("sort_definition_id"),
    sortDirection: text("sort_direction").notNull().default("asc"),
    governingOperationId: uuid("governing_operation_id").notNull(),
    createdByMembershipId: uuid("created_by_membership_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    uniqueIndex("saved_list_versions_workspace_id_id_uq").on(table.workspaceId, table.id),
    uniqueIndex("saved_list_versions_workspace_list_version_uq").on(table.workspaceId, table.listId, table.definitionVersion),
    index("saved_list_versions_history_idx").on(table.workspaceId, table.listId, table.definitionVersion.desc()),
    foreignKey({ name: "saved_list_versions_list_fk", columns: [table.workspaceId, table.listId], foreignColumns: [savedLists.workspaceId, savedLists.id] }),
    foreignKey({ name: "saved_list_versions_creator_fk", columns: [table.workspaceId, table.createdByMembershipId], foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id] }),
    check("saved_list_versions_definition_check", sql`${table.definitionVersion}>0`),
    check("saved_list_versions_contract_check", sql`${table.contractVersion}='saved-list-filter.v1'`),
    check("saved_list_versions_ast_size_check", sql`jsonb_typeof(${table.filterAst})='object' and octet_length(${table.filterAst}::text)<=8192`),
    check("saved_list_versions_hash_check", sql`${table.filterAstHash} ~ '^[0-9a-f]{64}$'`),
    check("saved_list_versions_sort_check", sql`${table.sortDirection} in ('asc','desc') and ((${table.sortSource}='system' and ${table.sortFieldCode} is not null and ${table.sortFieldCode}=btrim(${table.sortFieldCode}) and char_length(${table.sortFieldCode}) between 1 and 64 and ${table.sortFieldCode} !~ '[[:cntrl:]]' and ${table.sortDefinitionId} is null) or (${table.sortSource}='custom' and ${table.sortFieldCode} is null and ${table.sortDefinitionId} is not null))`),
  ],
);

export const onboardingProgress = pgTable(
  "onboarding_progress",
  {
    id: id(),
    userId: uuid("user_id").notNull().unique().references(() => users.id, { onDelete: "cascade" }),
    selectedPlanCode: text("selected_plan_code"),
    billingCadence: text("billing_cadence"),
    currentStep: text("current_step").notNull().default("account"),
    workspaceId: uuid("workspace_id").references(() => workspaces.id),
    completedAt: timestamp("completed_at", { withTimezone: true }),
    version: integer("version").notNull().default(1),
    ...timestamps,
  },
  (table) => [
    check("onboarding_billing_cadence_check", sql`${table.billingCadence} is null or ${table.billingCadence} in ('monthly', 'annual', 'sales_managed')`),
    check("onboarding_current_step_check", sql`${table.currentStep} in ('account', 'identity_verification', 'workspace', 'complete')`),
    check("onboarding_version_check", sql`${table.version} > 0`),
  ],
);

export const planCatalogEntries = pgTable(
  "plan_catalog_entries",
  {
    id: id(),
    code: text("code").notNull(),
    catalogVersion: text("catalog_version").notNull(),
    name: text("name").notNull(),
    status: text("status").notNull(),
    allowedCadences: jsonb("allowed_cadences").notNull(),
    includedActiveSeats: integer("included_active_seats").notNull(),
    currencyCode: text("currency_code"),
    billingUnit: text("billing_unit"),
    monthlyPriceCents: integer("monthly_price_cents"),
    annualMonthlyEquivalentPriceCents: integer("annual_monthly_equivalent_price_cents"),
    featureFlags: jsonb("feature_flags").notNull(),
    trialDays: integer("trial_days").notNull(),
    effectiveFrom: timestamp("effective_from", { withTimezone: true }).notNull(),
    effectiveTo: timestamp("effective_to", { withTimezone: true }),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("plan_catalog_code_version_uq").on(table.code, table.catalogVersion),
    check("plan_catalog_status_check", sql`${table.status} in ('draft', 'active', 'retired')`),
    check("plan_catalog_seats_check", sql`${table.includedActiveSeats} > 0`),
    check("plan_catalog_trial_days_check", sql`${table.trialDays} >= 0`),
    check(
      "plan_catalog_pricing_tuple_check",
      sql`num_nonnulls(
        ${table.currencyCode},
        ${table.billingUnit},
        ${table.monthlyPriceCents},
        ${table.annualMonthlyEquivalentPriceCents}
      ) = 0 or (
        num_nonnulls(
          ${table.currencyCode},
          ${table.billingUnit},
          ${table.monthlyPriceCents},
          ${table.annualMonthlyEquivalentPriceCents}
        ) = 4
        and
        ${table.currencyCode} ~ '^[A-Z]{3}$'
        and ${table.billingUnit} = 'workspace_subscription'
        and ${table.monthlyPriceCents} > 0
        and ${table.annualMonthlyEquivalentPriceCents} > 0
      )`,
    ),
    check("plan_catalog_effective_dates_check", sql`${table.effectiveTo} is null or ${table.effectiveTo} > ${table.effectiveFrom}`),
    check(
      "plan_catalog_allowed_cadences_check",
      sql`jsonb_typeof(${table.allowedCadences}) = 'array' and ${table.allowedCadences} <@ '["monthly", "annual", "sales_managed"]'::jsonb`,
    ),
  ],
);

export const workspaceEntitlementSnapshots = pgTable(
  "workspace_entitlement_snapshots",
  {
    id: id(),
    workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
    planCode: text("plan_code").notNull(),
    catalogVersion: text("catalog_version").notNull(),
    effectiveFeatureFlags: jsonb("effective_feature_flags").notNull(),
    effectiveLimits: jsonb("effective_limits").notNull(),
    effectiveAt: timestamp("effective_at", { withTimezone: true }).defaultNow().notNull(),
    expiresAt: timestamp("expires_at", { withTimezone: true }),
    ...timestamps,
  },
  (table) => [index("entitlement_workspace_idx").on(table.workspaceId)],
);

export const sessions = pgTable(
  "sessions",
  {
    id: id(),
    userId: uuid("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    activeWorkspaceId: uuid("active_workspace_id").references(() => workspaces.id, { onDelete: "set null" }),
    sessionHash: text("session_hash").notNull().unique(),
    securityVersion: integer("security_version").notNull().default(1),
    lastSeenAt: timestamp("last_seen_at", { withTimezone: true }).defaultNow().notNull(),
    idleExpiresAt: timestamp("idle_expires_at", { withTimezone: true }).notNull(),
    absoluteExpiresAt: timestamp("absolute_expires_at", { withTimezone: true }).notNull(),
    revokedAt: timestamp("revoked_at", { withTimezone: true }),
    authenticatedAt: timestamp("authenticated_at", { withTimezone: true }).defaultNow().notNull(),
    authMethod: text("auth_method").notNull().default("legacy"),
    ...timestamps,
  },
  (table) => [
    index("sessions_user_active_idx").on(table.userId, table.revokedAt, table.lastSeenAt),
    check("sessions_auth_method_check", sql`${table.authMethod} in ('password', 'google', 'fixture', 'legacy')`),
  ],
);

export const identityTokens = pgTable(
  "identity_tokens",
  {
    id: id(),
    userId: uuid("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    purpose: text("purpose").notNull(),
    tokenHash: text("token_hash").notNull().unique(),
    expiresAt: timestamp("expires_at", { withTimezone: true }).notNull(),
    consumedAt: timestamp("consumed_at", { withTimezone: true }),
    replacedAt: timestamp("replaced_at", { withTimezone: true }),
    ...timestamps,
  },
  (table) => [
    index("identity_tokens_user_purpose_idx").on(table.userId, table.purpose),
    check("identity_tokens_purpose_check", sql`${table.purpose} in ('email_verification', 'password_reset')`),
    check("identity_tokens_terminal_state_check", sql`${table.consumedAt} is null or ${table.replacedAt} is null`),
  ],
);

export const oidcTransactions = pgTable("oidc_transactions", {
  id: id(),
  stateHash: text("state_hash").notNull().unique(),
  nonceHash: text("nonce_hash").notNull(),
  pkceVerifierHash: text("pkce_verifier_hash").notNull(),
  redirectUri: text("redirect_uri").notNull(),
  linkingUserId: uuid("linking_user_id").references(() => users.id, { onDelete: "cascade" }),
  expiresAt: timestamp("expires_at", { withTimezone: true }).notNull(),
  consumedAt: timestamp("consumed_at", { withTimezone: true }),
  ...timestamps,
});

export const rateLimitWindows = pgTable(
  "rate_limit_windows",
  {
    id: id(),
    action: text("action").notNull(),
    riskKeyHash: text("risk_key_hash").notNull(),
    windowStartedAt: timestamp("window_started_at", { withTimezone: true }).notNull(),
    attempts: integer("attempts").notNull().default(1),
    expiresAt: timestamp("expires_at", { withTimezone: true }).notNull(),
    ...timestamps,
  },
  (table) => [
    uniqueIndex("rate_limit_action_key_window_uq").on(table.action, table.riskKeyHash, table.windowStartedAt),
    check("rate_limit_action_check", sql`${table.action} in ('register', 'login', 'verify', 'verification_resend', 'reset_request', 'reset_complete', 'invite_create', 'invite_resend', 'invite_accept', 'invite_revoke', 'member_change', 'team_change', 'recent_auth')`),
    check("rate_limit_attempts_check", sql`${table.attempts} > 0`),
  ],
);

export const idempotencyRecords = pgTable(
  "idempotency_records",
  {
    id: id(),
    principalKey: text("principal_key").notNull(),
    operation: text("operation").notNull(),
    idempotencyKey: text("idempotency_key").notNull(),
    requestHash: text("request_hash").notNull(),
    outcome: jsonb("outcome").notNull(),
    expiresAt: timestamp("expires_at", { withTimezone: true }).notNull(),
    ...timestamps,
  },
  (table) => [uniqueIndex("idempotency_principal_operation_key_uq").on(table.principalKey, table.operation, table.idempotencyKey)],
);

export const outboxMessages = pgTable(
  "outbox_messages",
  {
    id: id(),
    workspaceId: uuid("workspace_id").references(() => workspaces.id, { onDelete: "cascade" }),
    topic: text("topic").notNull(),
    aggregateType: text("aggregate_type").notNull(),
    aggregateId: uuid("aggregate_id"),
    operationId: uuid("operation_id"),
    resultVersion: integer("result_version"),
    payload: jsonb("payload").notNull(),
    status: text("status").notNull().default("pending"),
    attempts: integer("attempts").notNull().default(0),
    availableAt: timestamp("available_at", { withTimezone: true }).defaultNow().notNull(),
    leaseUntil: timestamp("lease_until", { withTimezone: true }),
    providerMessageId: text("provider_message_id"),
    providerIdempotencyKey: text("provider_idempotency_key").unique(),
    leaseOwner: text("lease_owner"),
    leaseGeneration: integer("lease_generation").notNull().default(0),
    lastError: text("last_error"),
    ...timestamps,
  },
  (table) => [
    index("outbox_pending_idx").on(table.status, table.availableAt),
    index("outbox_workspace_idx").on(table.workspaceId),
    uniqueIndex("outbox_operation_event_uq").on(table.workspaceId, table.topic, table.aggregateType, table.aggregateId, table.operationId, table.resultVersion).where(sql`${table.workspaceId} is not null and ${table.aggregateId} is not null and ${table.operationId} is not null and ${table.resultVersion} is not null`),
    check("outbox_status_check", sql`${table.status} in ('pending', 'processing', 'retry', 'delivered', 'dead_letter')`),
    check("outbox_attempts_check", sql`${table.attempts} >= 0`),
    check("outbox_lease_generation_check", sql`${table.leaseGeneration} >= 0`),
    check("outbox_result_version_check", sql`${table.resultVersion} is null or ${table.resultVersion}>0`),
  ],
);

export const auditEvents = pgTable(
  "audit_events",
  {
    id: id(),
    occurredAt: timestamp("occurred_at", { withTimezone: true }).defaultNow().notNull(),
    workspaceId: uuid("workspace_id").references(() => workspaces.id),
    actorUserId: uuid("actor_user_id").references(() => users.id),
    actorMembershipId: uuid("actor_membership_id"),
    actorType: text("actor_type").notNull(),
    sessionId: uuid("session_id").references(() => sessions.id),
    action: text("action").notNull(),
    targetType: text("target_type").notNull(),
    targetId: uuid("target_id"),
    outcome: text("outcome").notNull(),
    reasonCode: text("reason_code"),
    requestId: text("request_id"),
    correlationId: text("correlation_id"),
    sourceIp: text("source_ip"),
    sourceIpPolicy: text("source_ip_policy").notNull().default("omitted"),
    userAgentSanitized: text("user_agent_sanitized"),
    before: jsonb("before"),
    after: jsonb("after"),
    metadataVersion: integer("metadata_version").notNull().default(1),
    metadata: jsonb("metadata").notNull().default({}),
  },
  (table) => [
    index("audit_events_workspace_target_action_occurred_idx")
      .on(table.workspaceId, table.targetType, table.targetId, table.action, table.occurredAt, table.id)
      .where(sql`${table.workspaceId} is not null and ${table.targetId} is not null`),
    foreignKey({
      name: "audit_actor_workspace_membership_fk",
      columns: [table.workspaceId, table.actorMembershipId],
      foreignColumns: [workspaceMemberships.workspaceId, workspaceMemberships.id],
    }),
    check("audit_actor_type_check", sql`${table.actorType} in ('user', 'system', 'support')`),
    check("audit_outcome_check", sql`${table.outcome} in ('success', 'denied', 'failure')`),
    check("audit_actor_membership_scope_check", sql`${table.actorMembershipId} is null or ${table.workspaceId} is not null`),
    check("audit_source_ip_policy_check", sql`${table.sourceIpPolicy} in ('omitted', 'truncated', 'hashed')`),
    check("audit_source_ip_presence_check", sql`(${table.sourceIpPolicy} = 'omitted' and ${table.sourceIp} is null) or (${table.sourceIpPolicy} <> 'omitted' and ${table.sourceIp} is not null)`),
    check("audit_user_agent_sanitized_check", sql`${table.userAgentSanitized} is null or (length(${table.userAgentSanitized}) <= 512 and ${table.userAgentSanitized} !~ '[[:cntrl:]]')`),
    check("audit_before_safe_shape_check", sql`${table.before} is null or jsonb_typeof(${table.before}) in ('object', 'array')`),
    check("audit_after_safe_shape_check", sql`${table.after} is null or jsonb_typeof(${table.after}) in ('object', 'array')`),
    check("audit_metadata_version_check", sql`${table.metadataVersion} > 0`),
    check(
      "audit_metadata_allowlist_check",
      sql`jsonb_typeof(${table.metadata}) = 'object' and (${table.metadata} - array['risk_bucket', 'change_fields', 'provider', 'auth_method', 'policy_version', 'operation', 'invitation_generation', 'assigned_role', 'team_count', 'expected_version', 'result_version', 'seat_limit', 'active_seats', 'auth_age_bucket', 'selection_version']::text[]) = '{}'::jsonb`,
    ),
  ],
);
