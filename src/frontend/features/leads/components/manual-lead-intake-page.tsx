import{ManualLeadIntakeForm}from"./manual-lead-intake-form";
import{Panel}from"@/frontend/design-system";
export function ManualLeadIntakePage({workspaceId,receivedAt}:{workspaceId:string;receivedAt:string}){return <section className="admin-content narrow-admin p1a-feature"><header className="product-page-header"><div><p className="eyebrow">Leads / New</p><h1>Add lead</h1><p className="lead">Record an inquiry and preserve its original source separately from how NexaFlow received it.</p></div></header><div className="ds-form-layout"><Panel title="Lead inquiry" description="Company, subject, message, and attribution context are optional unless the server reports otherwise."><ManualLeadIntakeForm workspaceId={workspaceId} receivedAt={receivedAt}/></Panel></div></section>}
