import type {ModuleTransaction} from "../database";
export async function writeLeadScreenEvent(tx:ModuleTransaction,input:{workspaceId:string;leadId:string;version:number;requestId:string;operationId:string;action:string;fields:string[]}){if(!new Set(["crm.lead.profile_created","crm.lead.profile_updated"]).has(input.action))throw new Error("invalid_lead_screen_event");await tx.query(`insert into outbox_messages(workspace_id,topic,aggregate_type,aggregate_id,operation_id,result_version,payload) values($1,$2,'lead',$3,$4,$5,$6)`,[input.workspaceId,`${input.action}.v1`,input.leadId,input.operationId,input.version,JSON.stringify({schemaVersion:1,workspaceId:input.workspaceId,leadId:input.leadId,version:input.version,requestId:input.requestId,changeFields:[...new Set(input.fields)]})]);}
