import{z}from"zod";import{getServerEnv}from"@/server/env";import{localDatabase,mutationGuard}from"@/server/http";import{acceptInvitation,invitationDestinationByToken}from"@/server/tenant-admin/invitations";import{auditedFailure,enforceTenantRate,idempotencyKey,identity,success}from"@/server/tenant-admin/http";import{TenantAdminError}from"@/server/tenant-admin/permissions";
export async function POST(request:Request){const blocked=mutationGuard(request);if(blocked)return blocked;const{pool}=localDatabase(),env=getServerEnv();try{const parsed=z.object({token:z.string().min(32).max(128)}).safeParse(await request.json().catch(()=>null));if(!parsed.success)throw new TenantAdminError("invitation_invalid",410);const actor=await identity(pool,request);await enforceTenantRate(pool,request,"invite_accept",actor,await invitationDestinationByToken(pool,parsed.data.token,env.SESSION_SECRET));return success(await acceptInvitation(pool,{...actor,token:parsed.data.token,idempotencyKey:idempotencyKey(request),secret:env.SESSION_SECRET}))}catch(e){return auditedFailure(pool,request,e,{action:"workspace.invitation_accept_denied",targetType:"invitation"})}finally{await pool.end()}}
