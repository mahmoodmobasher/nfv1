import{z}from"zod";import{localDatabase,sessionToken,mutationGuard}from"@/server/http";import{getServerEnv}from"@/server/env";import{resolveIdentityContext}from"@/server/security/session";import{savePlanSelection}from"@/server/workspaces/provision";import{NextResponse}from"next/server";export async function POST(r:Request){const rejected=mutationGuard(r);if(rejected)return rejected;const env=getServerEnv(),{pool}=localDatabase();try{const context=await resolveIdentityContext(pool,sessionToken(r),env.SESSION_SECRET);if(!context)return NextResponse.json({code:"unauthorized"},{status:401});const body=z.object({planCode:z.string(),cadence:z.string()}).safeParse(await r.json().catch(()=>null));if(!body.success)return NextResponse.json({code:"invalid_request"},{status:400});return NextResponse.json(await savePlanSelection(pool,context.userId,body.data.planCode,body.data.cadence))}catch{return NextResponse.json({code:"invalid_plan"},{status:400})}finally{await pool.end()}}
