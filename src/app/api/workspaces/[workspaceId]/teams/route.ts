import{z}from"zod";import{getServerEnv}from"@/server/env";import{localDatabase,mutationGuard}from"@/server/http";import{createTeam}from"@/server/tenant-admin/administration";import{teamsPage,pageOptions}from"@/server/tenant-admin/pagination";import{auditedFailure,enforceTenantRate,failure,idempotencyKey,success,tenant}from"@/server/tenant-admin/http";import{TenantAdminError}from"@/server/tenant-admin/permissions";
export async function GET(request:Request,{params}:{params:Promise<{workspaceId:string}>}){const{workspaceId}=await params,{pool}=localDatabase(),env=getServerEnv();try{return success(await teamsPage(pool,await tenant(pool,request,workspaceId),pageOptions(request,env.SESSION_SECRET)))}catch(e){return auditedFailure(pool,request,e,{action:"workspace.team_change_denied",targetType:"team"})}finally{await pool.end()}}
export async function POST(request:Request,{params}:{params:Promise<{workspaceId:string}>}){const blocked=mutationGuard(request);if(blocked)return blocked;const{workspaceId}=await params,{pool}=localDatabase();try{const parsed=z.object({name:z.string().trim().min(1).max(100)}).safeParse(await request.json().catch(()=>null));if(!parsed.success)throw new TenantAdminError("validation_failed",400);const context=await tenant(pool,request,workspaceId);await enforceTenantRate(pool,request,"team_change",context);return success(await createTeam(pool,{context,name:parsed.data.name,key:idempotencyKey(request)}),201)}catch(e){return failure(e)}finally{await pool.end()}}
