import { CONTACT_CREATE, contactCreateCommandV1Schema, createContact, customerGraphListQueryV1Schema, listCustomerGraph } from "@/backend/modules/customer-graph";import { graphRoute,parsed } from "@/backend/modules/customer-graph/presentation/customer-graph.route";
import { CONTACT_SCREEN_CREATE_V2, contactScreenCreateCommandV2Schema } from "@/backend/modules/screen-forms";
export async function GET(request:Request,{params}:{params:Promise<{workspaceId:string}>}){const{workspaceId}=await params;return graphRoute(request,workspaceId,async({pool,actor,requestId})=>{const url=new URL(request.url),bootstrap=url.searchParams.get("bootstrap");if(bootstrap!==null&&bootstrap!=="true"&&bootstrap!=="false")throw Object.assign(new Error("validation_failed"),{code:"validation_failed",status:400});const query=customerGraphListQueryV1Schema.safeParse({status:url.searchParams.get("status")??undefined,cursor:url.searchParams.get("cursor")??undefined,limit:url.searchParams.get("limit")?Number(url.searchParams.get("limit")):undefined,bootstrap:bootstrap==="true"});if(!query.success)throw Object.assign(new Error("validation_failed"),{code:"validation_failed",status:400});return listCustomerGraph(pool,actor,"contact",query.data,requestId)})}
export async function POST(request:Request,{params}:{params:Promise<{workspaceId:string}>}){const{workspaceId}=await params;return graphRoute(request,workspaceId,({pool,actor,requestId,key,body})=>{const version=(body as {contractVersion?:unknown}|null)?.contractVersion,command=version===CONTACT_SCREEN_CREATE_V2?parsed(contactScreenCreateCommandV2Schema,body,CONTACT_SCREEN_CREATE_V2):parsed(contactCreateCommandV1Schema,body,CONTACT_CREATE);return createContact(pool,{actor,requestId,key,command})},201,true)}
