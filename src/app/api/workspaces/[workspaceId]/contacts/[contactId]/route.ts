import { CONTACT_EDIT, contactEditCommandV1Schema, editContact, getCustomerGraph } from "@/backend/modules/customer-graph";import { graphRoute,parsed } from "@/backend/modules/customer-graph/presentation/customer-graph.route";
import { CONTACT_SCREEN_EDIT_V2, contactScreenEditCommandV2Schema } from "@/backend/modules/screen-forms";
export async function GET(request:Request,{params}:{params:Promise<{workspaceId:string;contactId:string}>}){const{workspaceId,contactId}=await params;return graphRoute(request,workspaceId,({pool,actor,requestId})=>getCustomerGraph(pool,actor,"contact",contactId,requestId))}
export async function PATCH(request:Request,{params}:{params:Promise<{workspaceId:string;contactId:string}>}){const{workspaceId,contactId}=await params;return graphRoute(request,workspaceId,({pool,actor,requestId,key,body})=>{const version=(body as {contractVersion?:unknown}|null)?.contractVersion,command=version===CONTACT_SCREEN_EDIT_V2?parsed(contactScreenEditCommandV2Schema,body,CONTACT_SCREEN_EDIT_V2):parsed(contactEditCommandV1Schema,body,CONTACT_EDIT);return editContact(pool,{actor,contactId,requestId,key,command})},200,true)}
