import { COMPANY_EDIT, companyEditCommandV1Schema, editCompany, getCustomerGraph } from "@/backend/modules/customer-graph";
import { graphRoute, parsed } from "@/backend/modules/customer-graph/presentation/customer-graph.route";
import { COMPANY_SCREEN_EDIT_V2, companyScreenEditCommandV2Schema } from "@/backend/modules/screen-forms";
export async function GET(request:Request,{params}:{params:Promise<{workspaceId:string;companyId:string}>}){const{workspaceId,companyId}=await params;return graphRoute(request,workspaceId,({pool,actor,requestId})=>getCustomerGraph(pool,actor,"company",companyId,requestId))}
export async function PATCH(request:Request,{params}:{params:Promise<{workspaceId:string;companyId:string}>}){const{workspaceId,companyId}=await params;return graphRoute(request,workspaceId,({pool,actor,requestId,key,body})=>{const version=(body as {contractVersion?:unknown}|null)?.contractVersion,command=version===COMPANY_SCREEN_EDIT_V2?parsed(companyScreenEditCommandV2Schema,body,COMPANY_SCREEN_EDIT_V2):parsed(companyEditCommandV1Schema,body,COMPANY_EDIT);return editCompany(pool,{actor,companyId,requestId,key,command})},200,true)}
