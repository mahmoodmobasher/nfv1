import { COMPANY_CREATE, companyCreateCommandV1Schema, createCompany, customerGraphListQueryV1Schema, listCustomerGraph } from "@/backend/modules/customer-graph";
import { graphRoute, parsed } from "@/backend/modules/customer-graph/presentation/customer-graph.route";
import { COMPANY_SCREEN_CREATE_V2, companyScreenCreateCommandV2Schema, screenProfileResultV1Schema } from "@/backend/modules/screen-forms";
export async function GET(request:Request,{params}:{params:Promise<{workspaceId:string}>}){const{workspaceId}=await params;return graphRoute(request,workspaceId,async({pool,actor,requestId})=>{const url=new URL(request.url),bootstrap=url.searchParams.get("bootstrap");if(bootstrap!==null&&bootstrap!=="true"&&bootstrap!=="false")throw Object.assign(new Error("validation_failed"),{code:"validation_failed",status:400});const query=customerGraphListQueryV1Schema.safeParse({status:url.searchParams.get("status")??undefined,cursor:url.searchParams.get("cursor")??undefined,limit:url.searchParams.get("limit")?Number(url.searchParams.get("limit")):undefined,bootstrap:bootstrap==="true"});if(!query.success)throw Object.assign(new Error("validation_failed"),{code:"validation_failed",status:400});return listCustomerGraph(pool,actor,"company",query.data,requestId)})}
export async function POST(request:Request,{params}:{params:Promise<{workspaceId:string}>}){const{workspaceId}=await params;return graphRoute(request,workspaceId,async({pool,actor,requestId,key,body})=>{const version=(body as {contractVersion?:unknown}|null)?.contractVersion,isScreen=version===COMPANY_SCREEN_CREATE_V2,command=isScreen?parsed(companyScreenCreateCommandV2Schema,body,COMPANY_SCREEN_CREATE_V2):parsed(companyCreateCommandV1Schema,body,COMPANY_CREATE),result=await createCompany(pool,{actor,requestId,key,command});return isScreen?screenProfileResultV1Schema.parse({contractVersion:"screen-profile-result.v1",kind:"company",recordId:result.companyId,version:result.version,replayed:result.replayed,requestId:result.requestId}):result},201,true)}
