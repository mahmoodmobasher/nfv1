import{z}from"zod";import{getServerEnv}from"@/server/env";import{localDatabase,mutationGuard}from"@/server/http";import{settingsModel}from"@/server/tenant-admin/read-models";import{updateWorkspaceSettings}from"@/server/tenant-admin/extended";import{auditedFailure,enforceTenantRate,failure,idempotencyKey,success,tenant}from"@/server/tenant-admin/http";import{TenantAdminError}from"@/server/tenant-admin/permissions";
export async function GET(request:Request,{params}:{params:Promise<{workspaceId:string}>}){const{workspaceId}=await params,{pool}=localDatabase();try{return success(await settingsModel(pool,await tenant(pool,request,workspaceId)))}catch(e){return await auditedFailure(pool,request,e,{action:"workspace.settings_change_denied",targetType:"workspace"})}finally{await pool.end()}}
export async function PATCH(request:Request,{params}:{params:Promise<{workspaceId:string}>}){const blocked=mutationGuard(request);if(blocked)return blocked;const{workspaceId}=await params,{pool}=localDatabase(),env=getServerEnv();try{const parsed=z.object({name:z.string().trim().min(1).max(120),expectedVersion:z.number().int().positive()}).safeParse(await request.json().catch(()=>null));if(!parsed.success)throw new TenantAdminError("validation_failed",400);const context=await tenant(pool,request,workspaceId);await enforceTenantRate(pool,request,"member_change",context,workspaceId);return success(await updateWorkspaceSettings(pool,{context,...parsed.data,key:idempotencyKey(request),recentMinutes:env.RECENT_AUTH_MINUTES}))}catch(e){return failure(e)}finally{await pool.end()}}
