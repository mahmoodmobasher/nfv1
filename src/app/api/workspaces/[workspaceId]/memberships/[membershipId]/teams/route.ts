import{z}from"zod";import{localDatabase,mutationGuard}from"@/server/http";import{assignMembershipTeams}from"@/server/tenant-admin/extended";import{auditedFailure,enforceTenantRate,idempotencyKey,success,tenant}from"@/server/tenant-admin/http";import{TenantAdminError}from"@/server/tenant-admin/permissions";export async function PUT(request:Request,{params}:{params:Promise<{workspaceId:string;membershipId:string}>}){const blocked=mutationGuard(request);if(blocked)return blocked;const{workspaceId,membershipId}=await params,{pool}=localDatabase();try{const parsed=z.object({teamIds:z.array(z.string().uuid()).max(100),expectedMembershipVersion:z.number().int().positive()}).safeParse(await request.json().catch(()=>null));if(!parsed.success)throw new TenantAdminError("validation_failed",400);const context=await tenant(pool,request,workspaceId);await enforceTenantRate(pool,request,"team_change",context,membershipId);return success(await assignMembershipTeams(pool,{context,membershipId,...parsed.data,key:idempotencyKey(request)}))}catch(e){return auditedFailure(pool,request,e,{action:"workspace.membership_change_denied",targetType:"membership"})}finally{await pool.end()}}
