import{z}from"zod";import{getServerEnv}from"@/server/env";import{localDatabase,mutationGuard}from"@/server/http";import{invitationDestinationById,resendInvitation}from"@/server/tenant-admin/invitations";import{auditedFailure,enforceTenantRate,idempotencyKey,success,tenant}from"@/server/tenant-admin/http";import{TenantAdminError}from"@/server/tenant-admin/permissions";
export async function POST(request:Request,{params}:{params:Promise<{workspaceId:string;invitationId:string}>}){const blocked=mutationGuard(request);if(blocked)return blocked;const{workspaceId,invitationId}=await params,{pool}=localDatabase(),env=getServerEnv();try{const parsed=z.object({expectedVersion:z.number().int().positive()}).safeParse(await request.json().catch(()=>null));if(!parsed.success)throw new TenantAdminError("validation_failed",400);const context=await tenant(pool,request,workspaceId);await enforceTenantRate(pool,request,"invite_resend",context,await invitationDestinationById(pool,workspaceId,invitationId));return success(await resendInvitation(pool,{context,invitationId,...parsed.data,idempotencyKey:idempotencyKey(request),secret:env.SESSION_SECRET,appOrigin:env.APP_ORIGIN,ttlHours:env.INVITATION_TTL_HOURS}),202)}catch(e){return auditedFailure(pool,request,e,{action:"workspace.invitation_admin_denied",targetType:"invitation"})}finally{await pool.end()}}
