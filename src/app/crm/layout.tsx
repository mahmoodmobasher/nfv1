import{cookies}from"next/headers";import{redirect}from"next/navigation";import{createDb}from"@/server/db/client";import{getServerEnv}from"@/server/env";import{resolveIdentityContext}from"@/server/security/session";import{workspaceSummary}from"@/server/workspaces/provision";import{selectableWorkspaces}from"@/server/workspaces/selection";
export const dynamic="force-dynamic";
export default async function CrmLayout({children}:{children:React.ReactNode}){const env=getServerEnv(),token=(await cookies()).get(env.SESSION_COOKIE_NAME)?.value,{pool}=createDb();try{const identity=await resolveIdentityContext(pool,token,env.SESSION_SECRET);if(!identity)redirect("/login?next=/crm");let workspace=await workspaceSummary(pool,identity.userId,identity.activeWorkspaceId);if(!workspace&&!identity.activeWorkspaceId){const options=await selectableWorkspaces(pool,{...identity,activeWorkspaceId:null}),selected=options.find(option=>option.current);if(selected)workspace=await workspaceSummary(pool,identity.userId,selected.id)}if(!workspace){const count=(await pool.query("select count(*)::int count from workspace_memberships m join workspaces w on w.id=m.workspace_id and w.status='active' where m.user_id=$1 and m.status='active'",[identity.userId])).rows[0].count;redirect(count?"/workspace/switch":"/workspace/create")}}finally{await pool.end()}return children}
