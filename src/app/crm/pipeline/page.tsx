import Link from "next/link";
import { CrmShell } from "../crm-shell";
import { LeadPipeline, LeadPresentationUnavailable } from "@/frontend/features/leads";
import { loadLeadPipelineStages, loadLeadSummaries } from "@/frontend/features/leads/server";
import { crmPageContext } from "@/server/crm/page";
export const dynamic="force-dynamic";export const metadata={title:"Pipeline | NexaFlow"};
export default async function Pipeline({searchParams}:{searchParams:Promise<{q?:string}>}){const{q:rawQ=""}=await searchParams,q=rawQ.slice(0,200),{pool,workspace,context}=await crmPageContext("/crm/pipeline");try{let content;try{const registry=await loadLeadPipelineStages(pool,context),stages=await Promise.all(registry.items.map(async stage=>({stage,view:await loadLeadSummaries(pool,context,{q,stageId:stage.stageId,limit:50})})));content=<LeadPipeline stages={stages}/>;}catch{content=<LeadPresentationUnavailable/>}return <CrmShell workspace={workspace.name} role={context.role}><section className="admin-content pipeline-content"><header className="product-page-header crm-page-heading"><div><p className="eyebrow">Local CRM</p><h1>Pipeline</h1><p className="lead">See every active stage in the server-defined order.</p></div><div className="product-page-actions"><Link className="primary link-button" href="/crm/leads/new">Add a lead</Link></div></header><form className="admin-toolbar crm-search-toolbar" action="/crm/pipeline"><label>Search leads<input name="q" type="search" defaultValue={q} placeholder="Name or masked contact"/></label><button className="secondary">Search</button>{q&&<Link className="secondary link-button" href="/crm/pipeline">Clear filters</Link>}</form>{content}</section></CrmShell>}finally{await pool.end()}}
