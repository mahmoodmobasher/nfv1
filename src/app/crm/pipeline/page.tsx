import Link from "next/link";
import { CrmShell } from "../crm-shell";
import { crmPageContext } from "@/server/crm/page";
import { listLeads, pipelineStages } from "@/server/crm/leads";

export const dynamic="force-dynamic";export const metadata={title:"Pipeline | NexaFlow"};
export default async function Pipeline({searchParams}:{searchParams:Promise<{q?:string}>}){const{q=""}=await searchParams,{pool,workspace,context}=await crmPageContext("/crm/pipeline");try{const[leads,stages]=await Promise.all([listLeads(pool,context,q),pipelineStages(pool,context)]);return <CrmShell workspace={workspace.name} role={context.role}><section className="admin-content"><p className="eyebrow">Local CRM</p><header className="crm-page-heading"><div><h1>Pipeline</h1><p className="lead">See where leads are in your process and move work forward.</p></div><Link className="primary link-button" href="/crm/leads/new">Add a lead</Link></header><form className="admin-toolbar" action="/crm/pipeline"><label>Search leads<input name="q" type="search" defaultValue={q} placeholder="Name, email, or company"/></label><button className="secondary">Search</button>{q&&<Link className="secondary link-button" href="/crm/pipeline">Clear filters</Link>}</form><p role="status">{leads.length} {leads.length===1?"lead":"leads"} shown.</p>{leads.length===0?<div className="empty"><h2>{q?"No leads match these filters.":"Your pipeline is empty."}</h2><p>{q?"Clear filters to see all available leads.":"Add your first lead to start tracking customer work."}</p>{q?<Link className="secondary link-button" href="/crm/pipeline">Clear filters</Link>:<Link className="primary link-button" href="/crm/leads/new">Add a lead</Link>}</div>:<div className="pipeline-board">{stages.map(stage=>{const rows=leads.filter(lead=>lead.stage_id===stage.id);return <section className="pipeline-stage" key={stage.id} aria-labelledby={`stage-${stage.id}`}><h2 id={`stage-${stage.id}`}>{stage.name} <span>{rows.length}</span></h2>{rows.length===0?<p className="helper">No leads in this stage.</p>:rows.map(lead=><article className="lead-card" key={lead.id}><h3><Link href={`/crm/leads/${lead.id}`}>{lead.first_name} {lead.last_name}</Link></h3><p>{lead.company}</p><small>Owner: {lead.owner_name}</small><span>{lead.visibility==="workspace"?"Workspace visibility":"Team visibility"}</span><Link className="secondary link-button" href={`/crm/leads/${lead.id}`}>Change stage</Link></article>)}</section>})}</div>}</section></CrmShell>}finally{await pool.end()}}
