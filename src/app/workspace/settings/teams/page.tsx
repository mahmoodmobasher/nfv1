import{AdminShell}from"../admin-shell";import{TeamsClient}from"../admin-client";import{adminPageContext}from"@/server/tenant-admin/page";import{peopleModel,teamsModel}from"@/server/tenant-admin/read-models";export const dynamic="force-dynamic";export const metadata={title:"Teams | NexaFlow"};export default async function Page(){const{pool,workspace,context}=await adminPageContext();try{return <AdminShell workspace={workspace.name} role={context.role}><section className="admin-content narrow-admin"><p className="eyebrow">Workspace settings / Teams</p><h1>Teams</h1><p className="lead">Organize people for routing, visibility, and collaboration. Teams are optional.</p><TeamsClient workspaceId={workspace.id} initial={await teamsModel(pool,context)} people={await peopleModel(pool,context)}/></section></AdminShell>}finally{await pool.end()}}
