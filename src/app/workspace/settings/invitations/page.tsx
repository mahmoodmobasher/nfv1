import Link from"next/link";import{AdminShell}from"../admin-shell";import{InvitationsClient}from"../admin-client";import{adminPageContext}from"@/server/tenant-admin/page";import{invitationsModel}from"@/server/tenant-admin/read-models";export const dynamic="force-dynamic";export const metadata={title:"Invitations | NexaFlow"};export default async function Page(){const{pool,workspace,context}=await adminPageContext();try{return <AdminShell workspace={workspace.name} role={context.role}><section className="admin-content"><p className="eyebrow">Workspace settings / Invitations</p><h1>Invitations</h1><p className="lead">Review invitations, resend a message, or revoke access before it is accepted.</p><Link className="primary link-button" href="/workspace/settings/invite">Invite your team</Link><InvitationsClient workspaceId={workspace.id} initial={await invitationsModel(pool,context)}/></section></AdminShell>}finally{await pool.end()}}
