import{Shell}from"../../../onboarding/components";import{AcceptInvitationClient}from"./accept-client";export const metadata={title:"Accept workspace invitation | NexaFlow"};export default async function Page({searchParams}:{searchParams:Promise<{token?:string}>}){const{token}=await searchParams;return <Shell authLink={false}><p className="eyebrow">Workspace invitation</p>{token?<AcceptInvitationClient token={token}/>:<><h1>This invitation isn’t available.</h1><p>Request a new invitation from the workspace administrator.</p></>}</Shell>}
